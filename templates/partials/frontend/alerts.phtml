<?php

/** @var array $flashes */
if (!empty($flashes)) {
    ?>
    <div class="u-margin-bottom-l">
        <?php foreach ($flashes as $type => $messages) { ?>
            <?php
                // Maps flash type to BEM modifier
                $modifier = match ($type) {
                    'error' => 'danger',
                    'success' => 'success',
                    'warning' => 'warning',
                    default => 'info',
                };
            ?>
            <?php foreach ($messages as $msg) { ?>
                <div class="c-alert c-alert--<?php echo $modifier; ?> u-margin-bottom-s">
                    <?php echo \strip_tags($msg, '<strong><em><b><i><br><ul><li>'); ?>
                </div>
            <?php } ?>
        <?php } ?>
    </div>
<?php } ?>
