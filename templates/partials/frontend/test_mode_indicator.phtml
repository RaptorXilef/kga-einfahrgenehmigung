<?php

use App\Contracts\System\AssetHelperInterface;
use App\Infrastructure\Config\Config;

/**
 * Component: Test Mode & Admin Bypass Indicator (Animated)
 *
 * Zeigt einen globalen Warnbanner, wenn PayPal Sandbox oder der Dev-Admin-Modus aktiv sind.
 *
 * @var AssetHelperInterface $asset
 * @var Config $config
 * @var array $settings
 */
?>
<?php if ($config->isTestMode()) { ?>
    <div class="c-test-indicator">
        <div class="o-container o-container--large c-test-indicator__content">

            <div class="c-test-indicator__badge">
                <img src="<?php echo $asset->url('assets/img/icons/status-warning.webp'); ?>"
                     class="c-icon"
                     alt=""> Test-System
            </div>

            <div class="c-test-indicator__text">
                PayPal Sandbox aktiv &bull; Test-E-Mail:
                <code><?php echo \htmlspecialchars($config->get('mail')['recipients']['test']); ?></code>
            </div>

            <?php if ($config->get('admin_dev_mode')) { ?>
                <div class="c-test-indicator__admin-badge">
                    🔐 Admin Bypass Aktiv
                </div>
            <?php } ?>

        </div>
    </div>
<?php } ?>
