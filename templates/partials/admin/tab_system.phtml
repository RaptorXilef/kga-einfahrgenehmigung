<?php

use App\Contracts\Config\ConfigInterface;
use App\Contracts\System\AssetHelperInterface;
use App\Core\Service\AuthService;

/**
 * Partial: Dashboard Tab - System Maintenance & Cronjobs
 *
 * @var AssetHelperInterface $asset
 * @var AuthService $auth
 * @var ConfigInterface $config
 * @var array $settings
 */
$cronSecret = (string) $config->get('cron_secret', '');
$baseUrl = \rtrim($settings['base_url'] ?? '', '/');

$crons = [
    [
        'label' => 'Mail-Warteschlange abarbeiten',
        'desc' => 'Versendet zwischengespeicherte Mails (falls Queue-Limit im Web erreicht wird).',
        'url' => $baseUrl . '/api/process_mail_queue?token=' . $cronSecret,
        'interval' => 'Alle 1 - 5 Minuten',
        'icon' => '📧',
    ],
    [
        'label' => 'Zahlungserinnerungen versenden',
        'desc' => 'Prüft auf überfällige Anträge und versendet Mahnungen.',
        'url' => $baseUrl . '/api/cron/reminders?token=' . $cronSecret,
        'interval' => 'Täglich (z.B. morgens um 08:00)',
        'icon' => '🔔',
    ],
    [
        'label' => 'Archivierung & DSGVO',
        'desc' => 'Verschiebt alte Genehmigungen ins Archiv und anonymisiert >10 Jahre alte Daten.',
        'url' => $baseUrl . '/api/cron/archive?token=' . $cronSecret,
        'interval' => 'Täglich (z.B. nachts um 02:00)',
        'icon' => '🗄️',
    ],
    [
        'label' => 'Auto-Backup & Rotation',
        'desc' => 'Erstellt einen MySQL-Dump und löscht ältere Backups nach dem FIFO-Prinzip.',
        'url' => $baseUrl . '/api/cron/backup?token=' . $cronSecret,
        'interval' => 'Täglich (z.B. nachts um 03:00)',
        'icon' => '📦',
    ],
    [
        'label' => 'Spam-Filter synchronisieren',
        'desc' => 'Lädt die tagesaktuellen Trashmail-Domains über GitHub in den Cache herunter.',
        'url' => $baseUrl . '/api/cron/spam_sync?token=' . $cronSecret,
        'interval' => 'Einmal Wöchentlich',
        'icon' => '🛡️',
    ],
];
?>

<div class="c-tab-content-container">
    <?php if ($auth->hasPermission('system.maintenance.execute')) { ?>
        <div class="u-margin-bottom-l">
            <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                <span class="u-text-xl">🛠️</span>
                <div>
                    <h3 class="u-margin-none">Gefahrenzone: System-Werkzeuge</h3>
                    <p class="u-color-muted u-text-sm u-margin-none">Cache leeren und alte Daten anonymisieren.</p>
                </div>
            </header>
            <div class="o-grid o-grid--auto-fit-lg">
                <div class="c-card c-card--primary u-flex-column u-margin-none">
                    <div class="c-card__header u-flex u-align-center u-gap-s u-padding-around-m">
                        <h4 class="u-margin-none u-text-lg">🛡️ DSGVO-Anonymisierung</h4>
                    </div>
                    <div class="c-card__body u-flex u-flex-column u-gap-m u-flex-grow-1">
                        <p class="u-text-sm u-margin-none">Anonymisiert personenbezogene Daten in Archiv-Einträgen, die
                            älter als 10 Jahre sind. (Wird auch vom Cronjob erledigt).</p>
                        <form method="POST"
                              action="<?php echo $settings['base_url']; ?>anonymize_archive"
                              data-confirm="Sollen alte Archiv-Einträge jetzt DSGVO-konform anonymisiert werden?"
                              class="u-margin-top-auto">
                            <input type="hidden"
                                   name="csrf_token"
                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                            <button type="submit"
                                    class="c-button c-button--secondary u-width-100">Jetzt bereinigen</button>
                        </form>
                    </div>
                </div>

                <div class="c-card c-card--danger u-flex-column u-margin-none">
                    <div class="c-card__header u-flex u-align-center u-gap-s u-padding-around-m">
                        <h4 class="u-margin-none u-text-lg">🧹 System-Cache leeren</h4>
                    </div>
                    <div class="c-card__body u-flex u-flex-column u-gap-m u-flex-grow-1">
                        <p class="u-text-sm u-margin-none">Löscht temporäre Dateien, Routen-Caches und kompiliert die
                            System-Berechtigungen neu.</p>
                        <form method="POST"
                              action="<?php echo $settings['base_url']; ?>clear_cache"
                              data-confirm="Cache jetzt leeren?"
                              class="u-margin-top-auto">
                            <input type="hidden"
                                   name="csrf_token"
                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                            <button type="submit"
                                    class="c-button c-button--secondary u-width-100">Cache leeren</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    <?php } ?>

    <?php if ($auth->hasPermission('system.update.execute')) { ?>
        <div class="u-margin-bottom-l">
            <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                <span class="u-text-xl">🔄</span>
                <div>
                    <h3 class="u-margin-none">System-Updates</h3>
                    <p class="u-color-muted u-text-sm u-margin-none">Verwalten Sie Datenbank-Updates und
                        Schema-Erweiterungen.</p>
                </div>
            </header>
            <div class="o-grid o-grid--auto-fit-lg">
                <div class="c-card c-card--success u-flex-column u-margin-none">
                    <div class="c-card__header u-flex u-align-center u-gap-s u-padding-around-m">
                        <h4 class="u-margin-none u-text-lg">⚡ Datenbank-Migrationen</h4>
                    </div>
                    <div class="c-card__body u-flex u-flex-column u-gap-m u-flex-grow-1">
                        <p class="u-text-sm u-margin-none">Löst alle ausstehenden Tabellen-Anpassungen (.sql Dateien)
                            manuell aus, falls dies nicht via Console geschehen ist.</p>
                        <form method="POST"
                              action="<?php echo $settings['base_url']; ?>run_update_migrations"
                              class="u-margin-top-auto">
                            <input type="hidden"
                                   name="csrf_token"
                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                            <button type="submit"
                                    class="c-button c-button--primary u-width-100">Datenbank migrieren</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    <?php } ?>

    <?php if ($auth->hasPermission('system.maintenance.execute')) { ?>
        <div class="u-margin-top-xl">
            <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                <span class="u-text-xl">⏱️</span>
                <div>
                    <h3 class="u-margin-none">Cronjobs & Automatisierung</h3>
                    <p class="u-color-muted u-text-sm u-margin-none">Rufen Sie diese URLs über Ihren Webhoster (z.B.
                        Strato, Ionos, oder cron-job.org) zyklisch per GET auf.</p>
                </div>
            </header>

            <?php if ($cronSecret === '') { ?>
                <div class="c-alert c-alert--danger u-margin-bottom-m">
                    <strong>Achtung:</strong> Es wurde kein <code>cron_secret</code> in der Konfiguration definiert. Die
                    Automatisierung ist dadurch nicht abgesichert und wird fehlschlagen!
                </div>
            <?php } ?>

            <div class="u-flex u-flex-column u-gap-m">
                <?php foreach ($crons as $cron) { ?>
                    <div class="c-box c-box--highlight c-card__body u-flex u-flex-column u-gap-m u-margin-none">
                        <div>
                            <h4 class="u-margin-none u-flex u-align-center u-gap-s u-text-md">
                                <span><?php echo $cron['icon']; ?></span> <?php echo $cron['label']; ?>
                            </h4>
                            <p class="u-text-sm u-color-muted u-margin-top-xs u-margin-bottom-none"><?php echo $cron['desc']; ?></p>
                            <div class="u-margin-top-s">
                                <span class="c-badge c-badge--outline">Empfohlen: <?php echo $cron['interval']; ?></span>
                            </div>
                        </div>

                        <div class="u-flex u-align-center u-gap-s u-width-100">
                            <input type="text"
                                   readonly
                                   value="<?php echo \htmlspecialchars($cron['url']); ?>"
                                   class="c-form-input u-text-sm js-select-on-click u-color-muted u-font-mono"
                                   style="flex: 1; min-inline-size: 0; background-color: var(--color-bg-light);">
                            <button type="button"
                                    class="c-button c-button--secondary c-button--icon-only js-copy-btn"
                                    data-url="<?php echo \htmlspecialchars($cron['url']); ?>"
                                    title="In Zwischenablage kopieren">
                                <img src="<?php echo $asset->url('assets/img/icons/icon-clipboard.webp'); ?>"
                                     class="c-icon"
                                     alt="">
                            </button>
                            <button type="button"
                                    class="c-button c-button--primary js-cron-btn"
                                    data-url="<?php echo \htmlspecialchars($cron['url']); ?>"
                                    title="Jetzt manuell ausführen">
                                ▶ Ausführen
                            </button>
                        </div>
                    </div>
                <?php } ?>
            </div>
        </div>
    <?php } ?>
</div>
