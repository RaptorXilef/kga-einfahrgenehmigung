<?php

use App\Contracts\Storage\UserRepositoryInterface;
use App\Contracts\System\AssetHelperInterface;
use App\Core\Service\AuthService;
use App\Infrastructure\Storage\ImageStorageService;

/**
 * Partial: User Account Management
 *
 * Erstellung, Bearbeitung und Zuweisung von Benutzerkonten.
 *
 * @var AuthService $auth
 * @var UserRepositoryInterface $userRepository
 * @var ImageStorageService $imageStorage
 * @var AssetHelperInterface $asset
 * @var array $settings
 * @var array $roles
 * @var array $users
 * @var string $appRoot
 */
if ($auth->hasPermission('system.users.manage')) { ?>
    <div class="u-flex u-flex-column u-gap-l">
        <section class="c-card c-card--success">
            <header class="c-card__header">
                <h3 class="u-margin-none">
                    <img src="<?php echo $asset->url('assets/img/icons/icon-user.webp'); ?>"
                         class="c-icon"
                         alt=""> Neues Benutzerkonto anlegen
                </h3>
            </header>
            <div class="c-card__body u-padding-around-l">
                <form method="POST"
                      action="save_user"
                      enctype="multipart/form-data"
                      class="o-grid u-gap-m"
                      style="grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));">
                    <input type="hidden"
                           name="csrf_token"
                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                    <div class="c-form-group">
                        <label class="c-form-label">Benutzername</label>
                        <input type="text"
                               name="username"
                               class="c-form-input"
                               placeholder="z.B. max_m"
                               required>
                    </div>
                    <div class="c-form-group">
                        <label class="c-form-label">Passwort</label>
                        <input type="password"
                               name="password"
                               class="c-form-input"
                               placeholder="Passwort"
                               required>
                    </div>
                    <div class="c-form-group">
                        <label class="c-form-label">Wiederholen</label>
                        <input type="password"
                               name="password_repeat"
                               class="c-form-input"
                               placeholder="Bestätigen"
                               required>
                    </div>
                    <div class="c-form-group">
                        <label class="c-form-label">Rolle</label>
                        <select name="group"
                                class="c-form-select">
                            <?php foreach ($roles as $id => $r) { ?>
                                <option value="<?php echo $id; ?>"><?php echo \htmlspecialchars($r->name ?? 'Unbenannt'); ?></option>
                            <?php } ?>
                        </select>
                    </div>
                    <div class="u-flex u-align-end">
                        <button type="submit"
                                class="c-button c-button--primary"
                                style="block-size: 46px;">Erstellen</button>
                    </div>
                    <div class="u-margin-top-s u-flex u-align-center u-gap-m"
                         style="grid-column: 1 / -1;">
                        <div class="c-dropzone c-dropzone--preview js-avatar-dropzone js-preview-only u-position-relative u-text-center"
                             style="inline-size: 60px; block-size: 60px; border-radius: 50%; display: flex; align-items: center; justify-content: center; background-color: var(--color-bg-light); overflow: hidden; flex-shrink: 0;">
                            <img class="js-preview-img u-hidden"
                                 style="inline-size: 100%; block-size: 100%; object-fit: cover;">
                            <span class="js-preview-text u-opacity-50">
                            <img src="<?php echo $asset->url('assets/img/icons/icon-user.webp'); ?>" class="c-icon c-icon--md" alt="">
                        </span>
                            <div class="c-dropzone__overlay">Ablegen</div>
                            <input type="file"
                                   name="avatar"
                                   class="u-hidden"
                                   accept="image/*">
                        </div>
                        <div>
                            <label class="c-form-label u-margin-none">Profilbild (optional)</label>
                            <p class="u-color-muted u-text-sm u-margin-none">Klicken oder Bild hierher ziehen</p>
                        </div>
                    </div>
                </form>
            </div>
        </section>

        <div class="u-flex u-flex-column u-gap-m">
            <h3 class="u-margin-none u-margin-left-s">Registrierte Konten (<?php echo \count($users); ?>)</h3>
            <?php foreach ($users as $userId => $u) {
                $userImage = $imageStorage->getImageUrl('user_images', $userId, 'icon-user-default.webp');
                $roleData = $roles[$u->roleId] ?? null;
                $displayLogin = $u->username ?? $userId; ?>
                <div class="category-card is-closed"
                     id="card-<?php echo $userId; ?>">
                    <header class="category-header u-flex u-justify-between u-align-center"
                            style="background-color: var(--color-bg-white); padding: 12px 20px; border-radius: 12px;">
                        <div class="u-flex u-align-center u-gap-m js-toggle-parent u-cursor-pointer u-flex-grow-1">
                            <img src="<?php echo $userImage; ?>"
                                 style="inline-size: 38px; block-size: 38px; border-radius: 50%;">
                            <div>
                                <strong style="font-size: 1.05rem; color: var(--color-text-main);"><?php echo \htmlspecialchars((string) $displayLogin); ?></strong>
                                <div class="u-text-xs u-color-muted"><?php echo \htmlspecialchars($roleData ? $roleData->name : 'Unbekannt'); ?>
                                    <span class="u-opacity-50 u-font-mono">(ID: <?php echo \htmlspecialchars($userId); ?>)</span>
                                </div>
                            </div>
                        </div>
                        <?php $confirmMsg = 'Soll der Benutzer \'' . $displayLogin . '\' wirklich gelöscht werden?'; ?>
                        <form method="POST"
                              action="delete_user"
                              data-confirm="<?php echo \htmlspecialchars($confirmMsg, \ENT_QUOTES, 'UTF-8'); ?>"
                              style="margin: 0;">
                            <input type="hidden"
                                   name="csrf_token"
                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                            <input type="hidden"
                                   name="user_id"
                                   value="<?php echo $userId; ?>">
                            <button type="submit"
                                    class="c-button c-button--danger c-button--icon-only">
                                <img src="<?php echo $asset->url('assets/img/icons/action-delete.webp'); ?>"
                                     class="c-icon"
                                     alt="">
                            </button>
                        </form>
                    </header>
                    <div class="category-body u-padding-around-l"
                         style="background-color: var(--color-bg-light); border: 1px solid var(--color-border); border-block-start: none; border-end-start-radius: 12px; border-end-end-radius: 12px;">
                        <div class="o-grid u-gap-l"
                             style="grid-template-columns: 200px 1.2fr 1fr;">
                            <div class="u-text-center c-dropzone c-dropzone--preview js-avatar-dropzone js-preview-only"
                                 style="padding: 10px; border-radius: 12px;">
                                <label class="c-form-label u-justify-center">Profilbild</label>
                                <div class="u-position-relative u-display-inline-block">
                                    <img src="<?php echo $userImage; ?>"
                                         class="js-preview-img"
                                         style="inline-size: 120px; block-size: 120px; border-radius: 12px; border: 4px solid var(--color-bg-white); box-shadow: var(--shadow-md); margin-block-end: 10px; object-fit: cover;">
                                    <div class="c-dropzone__overlay u-margin-bottom-s">Foto ablegen</div>
                                </div>
                                <form method="POST"
                                      action="upload_avatar"
                                      enctype="multipart/form-data">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="user_id"
                                           value="<?php echo $userId; ?>">
                                    <input type="file"
                                           name="avatar"
                                           class="u-hidden"
                                           id="file-<?php echo \md5($userId); ?>"
                                           accept="image/*">
                                    <button type="button"
                                            class="c-button c-button--secondary c-button--small u-width-100 js-trigger-click"
                                            data-target="file-<?php echo \md5($userId); ?>">Bild ändern</button>
                                </form>
                                <hr style="border:none; border-top:1px solid #eee; margin:20px 0;">
                                <form method="POST"
                                      action="rename_user">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="user_id"
                                           value="<?php echo $userId; ?>">
                                    <label class="c-form-label">Login-Name</label>
                                    <div class="u-flex u-gap-xs">
                                        <input type="text"
                                               name="new_username"
                                               class="c-form-input"
                                               style="block-size:34px;"
                                               value="<?php echo \htmlspecialchars((string) $u->username); ?>">
                                        <button type="submit"
                                                class="c-button c-button--primary c-button--small">OK</button>
                                    </div>
                                </form>

                            </div>
                            <div class="u-flex u-flex-column u-gap-m">
                                <form method="POST"
                                      action="change_user_password"
                                      class="u-flex u-flex-column u-gap-s u-margin-top-m">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="user_id"
                                           value="<?php echo $userId; ?>">
                                    <label class="c-form-label">Passwort ändern</label>
                                    <input type="password"
                                           name="password"
                                           class="c-form-input"
                                           style="block-size:34px;"
                                           placeholder="Neues Passwort"
                                           required>
                                    <input type="password"
                                           name="password_repeat"
                                           class="c-form-input"
                                           style="block-size:34px;"
                                           placeholder="Wiederholen"
                                           required>
                                    <button type="submit"
                                            class="c-button c-button--danger c-button--small">Passwort setzen</button>
                                </form>
                            </div>
                            <div class="u-flex u-flex-column u-gap-m">
                                <form method="POST"
                                      action="change_user_role"
                                      class="u-flex u-flex-column u-gap-s">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="user_id"
                                           value="<?php echo $userId; ?>">
                                    <label class="c-form-label">Rolle zuweisen</label>
                                    <select name="group"
                                            class="c-form-select js-auto-submit-select"
                                            style="block-size:34px;">
                                        <?php foreach ($roles as $rid => $r) { ?>
                                            <option value="<?php echo $rid; ?>"
                                                    <?php echo ($u->roleId === $rid) ? 'selected' : ''; ?>>
                                                <?php echo \htmlspecialchars($r->name); ?>
                                            </option>
                                        <?php } ?>
                                    </select>
                                    <p class="u-color-muted u-text-sm u-margin-top-xs u-margin-bottom-none">
                                        ℹ️ Änderungen an der Rolle wirken sich sofort auf die Berechtigungen aus.
                                    </p>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            <?php } ?>
        </div>
    </div>
<?php } ?>
