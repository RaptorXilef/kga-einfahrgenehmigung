<?php

use App\Contracts\System\AssetHelperInterface;
use App\Core\Service\AuthService;
use App\Infrastructure\Maintenance\BackupService;

/**
 * Partial: Dashboard Tab - Data Migration & Backups
 *
 * @var AssetHelperInterface $asset
 * @var AuthService $auth
 * @var BackupService $backupService
 * @var array $settings
 */
if ($auth->hasPermission('system.maintenance.execute') || $auth->hasPermission('system.update.execute')) {
    $targets = [
        'roles' => ['label' => 'Rechte-Rollen', 'icon' => '🛡️'],
        'magic_links' => ['label' => 'Login-Tokens', 'icon' => '🔗'],
        'mail_log' => ['label' => 'E-Mail Protokolle', 'icon' => '📧'],
        'mail_queue' => ['label' => 'Mail-Warteschlange', 'icon' => '📤'],
        'pending_verification' => ['label' => 'Warteraum (E-Mail)', 'icon' => '✉️'],
        'permits' => ['label' => 'Genehmigungen', 'icon' => '📄'],
        'permits_archive' => ['label' => 'Genehmigungs-Archiv', 'icon' => '🗄️'],
        'permits_cancelled' => ['label' => 'Stornierte Genehmigungen', 'icon' => '🚫'],
        'users' => ['label' => 'Benutzerkonten', 'icon' => '👥'],
        'verified_pending' => ['label' => 'Warteraum (Zahlung)', 'icon' => '💳'],
        'vouchers' => ['label' => 'Gutscheine', 'icon' => '🎟️'],
        'vouchers_archive' => ['label' => 'Gutschein-Archiv', 'icon' => '📚'],
        'login_attempts' => ['label' => 'Login-Versuche', 'icon' => '🛡'],
        'update_migrations' => ['label' => 'Update-Migration-Verlauf', 'icon' => '♻'],
        'audit_logs' => ['label' => 'Nutzerprotokoll (Audit)', 'icon' => '👁️‍🗨️'],
    ];
    ?>
    <div class="c-tab-content-container">

        <?php if ($auth->hasPermission('system.maintenance.execute')) { ?>
            <div class="u-margin-bottom-l">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">🛠️</span>
                    <div>
                        <h3 class="u-margin-none">Gefahrenzone: System-Werkzeuge</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Cache leeren, Datenbestände zurücksetzen und
                            alte Daten anonymisieren.</p>
                    </div>
                </header>

                <div class="u-grid"
                     style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">
                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--success-color); display: flex; flex-direction: column;">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">📦 Manuelles Voll-Backup</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                            <p class="u-text-small u-margin-none">Sichert sofort alle aktuellen Datenstände der
                                MySQL-Datenbank im Backup-Verzeichnis.</p>
                            <form method="POST"
                                  action="<?php echo $settings['base_url']; ?>create_backup"
                                  class="u-margin-top-auto">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <button type="submit"
                                        class="c-button c-button--success u-width-100"
                                        style="font-size: 0.9rem;"> Backup jetzt erstellen </button>
                            </form>
                        </div>
                    </div>

                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--info-color); display: flex; flex-direction: column;">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">🛡️ DSGVO-Anonymisierung</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                            <p class="u-text-small u-margin-none">Anonymisiert personenbezogene Daten in
                                Archiv-Einträgen, die älter als 10 Jahre sind.</p>
                            <form method="POST"
                                  action="<?php echo $settings['base_url']; ?>anonymize_archive"
                                  onsubmit="return confirm('Sollen alte Archiv-Einträge jetzt DSGVO-konform anonymisiert werden?')"
                                  class="u-margin-top-auto">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <button type="submit"
                                        class="c-button c-button--secondary u-width-100"
                                        style="font-size: 0.9rem;"> Jetzt prüfen & bereinigen </button>
                            </form>
                        </div>
                    </div>

                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--danger-color); display: flex; flex-direction: column;">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">🧹 System-Cache</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                            <p class="u-text-small u-margin-none">Löscht temporäre Dateien und kompiliert die
                                System-Berechtigungen neu.</p>
                            <form method="POST"
                                  action="<?php echo $settings['base_url']; ?>clear_cache"
                                  onsubmit="return confirm('Cache jetzt leeren?')"
                                  class="u-margin-top-auto">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <button type="submit"
                                        class="c-button c-button--secondary u-width-100"
                                        style="font-size: 0.9rem;"> Cache leeren </button>
                            </form>
                        </div>
                    </div>

                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--danger-color); display: flex; flex-direction: column;">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">🗑️ Datenbestand leeren</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                            <p class="u-text-small u-margin-none">Leert eine komplette Datenbank-Tabelle. Es wird vorab
                                automatisch ein Backup erstellt.</p>
                            <form method="POST"
                                  action="<?php echo $settings['base_url']; ?>truncate_target"
                                  onsubmit="return confirm('WARNUNG: Die gewählten Daten werden unwiderruflich GELÖSCHT! Fortfahren?')"
                                  class="u-margin-top-auto u-flex u-flex-column u-gap-s">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <div class="u-flex u-gap-s">
                                    <select name="target"
                                            class="c-form-select"
                                            required>
                                        <option value="">-- Bereich wählen --</option>
                                        <?php foreach ($targets as $key => $data) { ?>
                                            <option value="<?php echo $key; ?>"><?php echo $data['label']; ?></option>
                                        <?php } ?>
                                    </select>
                                    <input type="hidden"
                                           name="engine"
                                           value="mysql">
                                    <button type="submit"
                                            class="c-button c-button--danger"
                                            style="width: auto; padding: 0 16px;"> Löschen </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        <?php } ?>

        <?php if ($auth->hasPermission('system.update.execute')) { ?>
            <div class="u-margin-bottom-l">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">🔄</span>
                    <div>
                        <h3 class="u-margin-none">System-Updates</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Verwalten Sie Datenbank-Updates und
                            Software-Versionen.</p>
                    </div>
                </header>

                <div class="u-grid"
                     style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">
                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--primary-color); display: flex; flex-direction: column;">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">🔄 Nach Updates suchen</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                            <p class="u-text-small u-margin-none">Umgeht den Cache und fragt sofort live bei GitHub an,
                                ob eine neue Version verfügbar ist.</p>
                            <form method="POST"
                                  action="<?php echo $settings['base_url']; ?>force_update_check"
                                  class="u-margin-top-auto">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <button type="submit"
                                        class="c-button c-button--primary u-width-100"
                                        style="font-size: 0.9rem;"> Jetzt live prüfen </button>
                            </form>
                        </div>
                    </div>

                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--info-color); display: flex; flex-direction: column;">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">⚡ Datenbank-Migrationen</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                            <p class="u-text-small u-margin-none">Löst alle ausstehenden Tabellen-Anpassungen
                                (00x_...php) aus.</p>
                            <form method="POST"
                                  action="<?php echo $settings['base_url']; ?>run_update_migrations"
                                  class="u-margin-top-auto">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <button type="submit"
                                        class="c-button c-button--secondary u-width-100"
                                        style="font-size: 0.9rem;"> Datenbank migrieren </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        <?php } ?>

        <?php if ($auth->hasPermission('system.maintenance.execute')) { ?>
            <div class="u-margin-top-xl">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">💾</span>
                    <div>
                        <h3 class="u-margin-none">Verfügbare Backups</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Übersicht aller Systemsicherungen zur
                            Wiederherstellung.</p>
                    </div>
                </header>

                <?php if (empty($backups)) { ?>
                    <div class="c-card"
                         style="border-top: 4px solid var(--json-color);">
                        <div class="c-card__body u-padding-around-l u-text-center u-text-muted">
                            <span style="font-size: 2rem; display: block; margin-bottom: 10px;">📦</span>
                            Es sind noch keine Backups im System vorhanden.
                        </div>
                    </div>
                <?php } else { ?>
                    <div class="u-grid"
                         style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">
                        <?php foreach ($backups as $ts => $files) {
                            $dateObj = \DateTimeImmutable::createFromFormat('Ymd-His', $ts);
                            $formattedDate = $dateObj ? $dateObj->format('d.m.Y — H:i:s') : $ts;
                            ?>
                            <div class="c-card"
                                 style="margin-bottom: 0; border-top: 4px solid var(--primary-color); display: flex; flex-direction: column;">
                                <div class="c-card__header"
                                     style="padding: 1rem 1.5rem;">
                                    <h4 class="u-margin-none"
                                        style="font-size: 1.1rem;"><?php echo $formattedDate; ?></h4>
                                    <span class="u-text-muted u-text-small"><?php echo $ts; ?></span>
                                </div>

                                <div class="c-card__body u-flex u-flex-column u-gap-m"
                                     style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                                    <div class="u-flex u-flex-wrap u-gap-xs">
                                        <?php foreach ($files as $file) {
                                            $cleanName = \str_replace(['_file.json', '_sql.json'], '', $file);
                                            ?>
                                            <span class="c-badge" style="background: var(--primary-soft); color: var(--primary-color); font-size: 0.7rem;">
                                            <?php echo \htmlspecialchars($cleanName); ?>
                                        </span>
                                        <?php } ?>
                                    </div>

                                    <form method="POST"
                                          action="<?php echo $settings['base_url']; ?>restore_data"
                                          onsubmit="return confirm('ACHTUNG: Dies überschreibt Ihre aktuellen Daten am Zielort mit dem alten Backup-Stand! Fortfahren?')"
                                          class="u-flex u-flex-column u-gap-s u-margin-top-auto">
                                        <input type="hidden"
                                               name="csrf_token"
                                               value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                        <input type="hidden"
                                               name="timestamp"
                                               value="<?php echo $ts; ?>">
                                        <input type="hidden"
                                               name="engine"
                                               value="mysql">
                                        <div class="u-flex u-gap-s">
                                            <select name="target"
                                                    class="c-form-select"
                                                    required>
                                                <option value="">-- Bereich --</option>
                                                <?php foreach ($targets as $key => $data) { ?>
                                                    <option value="<?php echo $key; ?>"><?php echo $data['label']; ?></option>
                                                <?php } ?>
                                            </select>
                                            <button type="submit"
                                                    class="c-button c-button--danger"
                                                    style="width: auto; padding: 0 16px; font-weight: 600;"> Restore
                                            </button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        <?php } ?>
                    </div>
                <?php } ?>
            </div>
        <?php } ?>
    </div>
<?php } ?>
