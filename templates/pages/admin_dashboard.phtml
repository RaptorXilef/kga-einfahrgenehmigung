<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - <?= htmlspecialchars($settings['vereins_name']) ?></title>
    <link rel="stylesheet" href="assets/css/main.min.css">
</head>
<body class="l-admin">
    <nav class="c-admin-nav">
        <div class="c-container">
            <span>Eingeloggt als: <strong><?= htmlspecialchars($_SESSION['admin_user']) ?></strong> (Stufe <?= $_SESSION['admin_level'] ?>)</span>
            <a href="?action=logout" class="u-float-right">Abmelden</a>
        </div>
    </nav>

    <main class="c-container u-padding-y-l">
        <h1>Verwaltung der Ausnahmegenehmigungen</h1>

        <section class="c-stats-grid u-margin-bottom-l">
            <div class="c-card c-card--info">Aktiv: <?= count($groups['active']) ?></div>
            <div class="c-card">Zukünftig: <?= count($groups['future']) ?></div>
        </section>

        <section class="c-card">
            <header class="c-card__header"><h3>Aktuelle Genehmigungen</h3></header>
            <table class="c-table">
                <thead>
                    <tr>
                        <th>Kennung</th>
                        <th>Name</th>
                        <th>Status</th>
                        <th>Aktion</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($groups['active'] as $p): ?>
                    <tr>
                        <td><code><?= $p->code ?></code></td>
                        <td><?= htmlspecialchars($p->name) ?></td>
                        <td>
                            <span class="c-badge c-badge--<?= $p->status === 'bezahlt' ? 'success' : 'warning' ?>">
                                <?= strtoupper($p->status) ?>
                            </span>
                        </td>
                        <td>
                            <?php if ($p->status !== 'bezahlt'): ?>
                            <form method="POST" style="display:inline;">
                                <input type="hidden" name="action" value="mark_as_paid">
                                <input type="hidden" name="code" value="<?= $p->code ?>">
                                <button type="submit" class="c-button c-button--success c-button--small">Zahlung eingegangen</button>
                            </form>
                            <?php endif; ?>
                            <a href="check.php?code=<?= $p->code ?>" target="_blank" class="c-button c-button--small">Details</a>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </section>
    </main>
</body>
</html>
