<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gültigkeitsprüfung - <?= htmlspecialchars($settings['vereins_name']) ?></title>
    <link rel="stylesheet" href="assets/css/main.min.css">
</head>
<body class="l-check u-bg-light">
    <main class="c-container u-text-center u-padding-top-xl">

        <?php if ($permit->isValid()): ?>
            <div class="c-card c-card--success u-margin-auto" style="max-width: 450px;">
                <div class="c-card__header">
                    <span class="u-font-xl">✅ GENEHMIGUNG GÜLTIG</span>
                </div>
                <div class="c-card__body">
                    <div class="u-margin-bottom">
                        <strong>Identifikation:</strong><br>
                        <?php
                            $info = [];
                            if (!empty($permit->kennzeichen)) $info[] = htmlspecialchars($permit->kennzeichen);
                            if (!empty($permit->firma)) $info[] = "Firma: " . htmlspecialchars($permit->firma);
                            echo implode(' / ', $info);
                        ?>
                    </div>
                    <div class="u-text-muted">
                        Gültigkeitszeitraum:<br>
                        <strong><?= $permit->von->format('d.m.Y') ?></strong> bis <strong><?= $permit->bis->format('d.m.Y') ?></strong>
                    </div>
                </div>
            </div>
        <?php else: ?>
            <div class="c-card c-card--danger u-margin-auto" style="max-width: 400px;">
                <div class="c-card__header">
                    <span class="u-font-xl">❌ UNGÜLTIG</span>
                </div>
                <div class="c-card__body">
                    <p>Diese Genehmigung ist abgelaufen, noch nicht gültig oder gesperrt.</p>
                </div>
            </div>
        <?php endif; ?>

        <?php if (!$showAdminView): ?>
            <div class="u-margin-top-l">
                <a href="admin.php?redirect_to_code=<?= $permit->code ?>" class="u-text-small">Vorstands-Login für Details</a>
            </div>
        <?php endif; ?>

        <footer class="u-margin-top-l">
            <p class="u-text-muted">SmartCode: <?= htmlspecialchars($permit->code) ?></p>
        </footer>
    </main>
</body>
</html>
