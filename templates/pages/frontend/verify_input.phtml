<?php

use App\Contracts\System\AssetHelperInterface;
use App\Infrastructure\Config\Config;

/**
 * Page: Verification Code Input
 *
 * Formular zur Eingabe des 6-stelligen Bestätigungscodes nach Antragstellung.
 *
 * Manuelle Eingabemaske für E-Mail-Verifikationscodes (Double-Opt-In).
 * Wird aufgerufen, wenn der Nutzer den Link nicht direkt klickt, sondern den 6-stelligen Code manuell eingibt.
 *
 * @var AssetHelperInterface $asset
 * @var Config $config
 * @var array $settings Globale Einstellungen (z.B. base_url für Icons, vereins_name).
 * @var bool $isError  Gibt an, ob die Nachricht ein Fehlerzustand ist (steuert CSS-Alert-Klassen).
 * @var string $appRoot
 * @var string $cspNonce
 */
$layout = 'public';
$pageTitle = 'E-Mail bestätigen - ' . ($settings['vereins_name'] ?? ''); ?>

<div class="c-tab-content-container o-container o-container--small u-padding-y-xl">
    <div class="c-card c-card--primary u-margin-auto">
        <header class="c-card__header u-text-center">
            <img src="<?php echo $asset->url('assets/img/icons/status-success.webp'); ?>"
                 class="c-icon c-icon--md"
                 alt="">
            <h2 class="u-margin-none u-margin-top-s">E-Mail bestätigen</h2>
            <p class="u-text-muted u-margin-top-s">Geben Sie den 6-stelligen Bestätigungscode aus Ihrer E-Mail ein, um
                den Antrag abzuschließen.</p>
        </header>

        <div class="c-card__body">
            <?php require $appRoot . '/templates/partials/frontend/alerts.phtml'; ?>

            <form method="POST"
                  action="verify"
                  class="c-form u-text-center">
                <input type="hidden"
                       name="csrf_token"
                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">

                <div class="c-form-group">
                    <input type="text"
                           name="verification_code"
                           aria-label="6-stelliger Bestätigungscode"
                           class="c-form-input c-form-input--code u-text-center"
                           placeholder="______"
                           required
                           autofocus
                           maxlength="6"
                           autocomplete="one-time-code">
                </div>

                <button type="submit"
                        name="submit_code"
                        class="c-button--primary c-button--submit u-width-100 u-margin-top-m">
                    Jetzt bestätigen
                    <img src="<?php echo $asset->url('assets/img/icons/status-success.webp'); ?>"
                         class="c-icon"
                         alt="">
                </button>
            </form>
        </div>
    </div>
</div>
