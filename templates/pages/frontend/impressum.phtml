<?php

use App\Contracts\System\AssetHelperInterface;
use App\Modules\System\Application\UseCases\ViewLegal\ImpressumViewDto;

/**
 * Page: Impressum
 * VSA FIX: 100% Dumme View - Konsumiert ausschließlich das stark typisierte ImpressumViewDto.
 *
 * @var AssetHelperInterface $asset
 * @var ImpressumViewDto $viewDto
 * @var array<string, mixed> $settings Basis-Konfigurationen (vereins_name, base_url).
 * @var string $appRoot Root-Pfad der Anwendung.
 * @var string $cspNonce
 * @var string $pageTitle
 */
$layout = 'public';
?>

<div class="c-tab-content-container o-container o-container--large u-padding-block-xl">
    <div class="c-document">
        <h1 class="c-document__title">
            <?php echo \htmlspecialchars($viewDto->title); ?>
        </h1>

        <p class="u-font-bold u-margin-block-end-none"><?php echo \htmlspecialchars($viewDto->clubName); ?></p>
        <p class="u-margin-block-start-xs"><?php echo \htmlspecialchars($viewDto->address); ?></p>

        <h3 class="c-document__subtitle u-margin-block-start-l u-margin-block-end-xs">Vertreten durch den Vorstand:</h3>
        <ul class="c-document__list">
            <?php foreach ($viewDto->boardMembers as $mitglied) { ?>
            <li><?php echo \htmlspecialchars($mitglied); ?></li>
            <?php } ?>
        </ul>

        <h3 class="c-document__subtitle u-margin-block-start-l u-margin-block-end-xs">Kontakt:</h3>
        <p class="u-margin-block-xs">Telefon: <?php echo \htmlspecialchars($viewDto->phone); ?></p>
        <p class="u-margin-block-xs">
            E-Mail:
            <a
                href="mailto:<?php echo \htmlspecialchars($viewDto->email, \ENT_QUOTES, 'UTF-8'); ?>"
                class="u-text-link"
            >
                <?php echo \htmlspecialchars($viewDto->email); ?>
            </a>
        </p>

        <h3 class="c-document__subtitle u-margin-block-start-l u-margin-block-end-xs">Registereintrag:</h3>
        <p class="u-margin-block-xs">Registergericht: <?php echo \htmlspecialchars($viewDto->registerCourt); ?>
        </p>
        <p class="u-margin-block-xs">Registernummer: <?php echo \htmlspecialchars($viewDto->registerNumber); ?></p>

        <?php if ($viewDto->hasUstId) { ?>
        <p class="u-margin-block-start-m"><strong>Umsatzsteuer-ID:</strong>
            <?php echo \htmlspecialchars($viewDto->ustId); ?></p>
        <?php } ?>

        <h3 class="c-document__subtitle u-margin-block-start-l u-margin-block-end-xs">Betreiber der Webseite:</h3>
        <p class="u-margin-block-xs"><?php echo \htmlspecialchars($viewDto->responsiblePersonName); ?></p>
        <p class="u-margin-block-xs u-text-muted u-text-sm">
            <?php echo \htmlspecialchars($viewDto->responsiblePersonAddress); ?></p>
    </div>
</div>
