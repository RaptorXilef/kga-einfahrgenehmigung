<?php

use App\Contracts\System\AssetHelperInterface;
use App\Modules\Permit\Application\UseCases\SubmitPermitRequest\PermitFormViewDto;

/**
 * Page: Public Application Form
 *
 * Das interaktive Antragsformular für neue Genehmigungen inkl. Code-Einlösung.
 * Komplett logikbefreites Template (Dumb View).
 *
 * @var AssetHelperInterface $asset
 * @var PermitFormViewDto $viewDto Enthält den vollständig aufgelösten Formulardaten-Status.
 * @var array<string, mixed> $settings Enthält gefilterte Templates.
 * @var string $vereinsName
 * @var bool $hasActiveVouchers True, falls einlösbare Gutscheine existieren.
 * @var bool $success True, wenn der Vorab-Antrag erfolgreich ausgelöst wurde.
 * @var string $appRoot System-Root-Pfad.
 * @var string $cspNonce
 * @var string $csrfToken
 * @var string|null $logoUrl Bereitgestellt durch TemplateRenderer.
 * @var string $message Erfolgs- oder Fehlermeldungen nach dem Absenden.
 */
$layout = 'public';
$pageTitle = 'Antrag - ' . $vereinsName;
?>

<div class="c-tab-content-container o-container o-container--large u-padding-block-l">
    <div class="c-card c-card--primary u-margin-inline-auto js-permit-form">
        <header class="c-card__header u-text-center">
            <?php if (!empty($logoUrl)) { ?>
            <div class="u-margin-block-end-m u-flex u-justify-center">
                <img
                    src="<?php echo $logoUrl; ?>"
                    alt="<?php echo \htmlspecialchars($vereinsName); ?>"
                    loading="lazy"
                    class="c-logo"
                >
            </div>
            <?php } ?>
            <h1 class="u-margin-none u-text-center">Antrag auf Ausnahmegenehmigung zum Befahren der Anlage</h1>
        </header>

        <div class="c-card__body c-card__body--xl">

            <!-- Bestätigungs Meldung - E-Mail versandt -->
            <?php if ($success) { ?>
            <div class="c-alert c-alert--success c-alert--large u-margin-block-end-l">
                <img
                    src="<?php echo $asset->url('assets/img/icons/success.webp'); ?>"
                    class="c-icon c-icon--lg u-margin-block-end-m"
                    loading="lazy"
                    alt=""
                >
                <h2 class="u-margin-none u-color-success-dark">E-Mail bestätigen!</h2>

                <p class="u-margin-block-start-m u-text-lg o-container o-container--small">
                    <?php echo \htmlspecialchars($message, \ENT_QUOTES, 'UTF-8'); ?>
                </p>

                <div class="c-alert__footer u-margin-block-start-xl">
                    <p>Sie werden in
                        <span
                            id="countdown"
                            class="u-font-extrabold u-text-xl u-color-primary"
                        > 60</span>
                        Sekunden zur Code-Eingabe weitergeleitet...
                    </p>
                    <a
                        href="verify"
                        class="c-button c-button--success c-button--wide u-margin-block-start-m"
                    >
                        Jetzt direkt zur Eingabe
                    </a>
                </div>
            </div>

            <script nonce="<?php echo $cspNonce; ?>">
            // TODO ggf. in JS-Dateien auslagern
            // 60 Sekunden Countdown
            let seconds = 60; // redirect Zeit
            const display = document.getElementById('countdown');
            const timer = setInterval(() => {
                seconds--;
                if (display) display.innerText = seconds;
                if (seconds <= 0) {
                    clearInterval(timer);
                    window.location.href = 'verify';
                }
            }, 1000);

            </script>
            <?php } else { ?>

            <?php require $appRoot . '/templates/partials/frontend/alerts.phtml'; ?>

            <!-- Warnung-Box für Datumsfehler -->
            <div
                id="date-warning"
                class="c-box c-box--warning u-margin-block-end-m js-pf-date-warning"
                hidden
            >
                <span
                    id="date-warning-text"
                    class="js-pf-date-warning-text"
                ></span>
            </div>

            <!-- GUTSCHEIN EINGABE / ANZEIGE -->
            <?php if ($viewDto->hasActiveVoucher) { ?>
            <div class="c-voucher-alert">
                <span class="u-text-xl"><img
                        src="<?php echo $asset->url('assets/img/icons/voucher.webp'); ?>"
                        class="c-icon"
                        loading="lazy"
                        alt=""
                    ></span>
                <div>
                    <strong>Aktivierungscode aktiv: <?php echo \htmlspecialchars($viewDto->voucherCode); ?></strong><br>
                    <small><?php echo \htmlspecialchars($viewDto->voucherReason); ?></small>
                </div>
            </div>
            <?php } elseif ($hasActiveVouchers) { ?>
            <!-- Gutschein Eingabefeld (Versteckt bis Klick) -->
            <div class="u-text-center u-margin-block-end-l u-flex u-justify-center">
                <button
                    type="button"
                    class="c-voucher-toggle js-voucher-toggle u-border-none"
                    style="background: transparent; padding: 0; font: inherit;"
                    aria-expanded="false"
                    aria-controls="voucher-container"
                >
                    <img
                        src="<?php echo $asset->url('assets/img/icons/voucher.webp'); ?>"
                        class="c-icon"
                        loading="lazy"
                        alt=""
                    >
                    Sie haben einen Aktivierungscode? Dann hier klicken.
                </button>
            </div>

            <div
                id="voucher-container"
                class="c-voucher-container js-voucher-container c-box c-box--dashed o-container o-container--small u-margin-inline-auto"
            >
                <!-- Formular nur für den Gutschein-Check -->
                <form
                    method="GET"
                    id="voucherRedeemForm"
                    action="index"
                    class="u-flex u-gap-s u-align-end js-form-voucher-redeem"
                >
                    <div class="c-form-group u-flex-grow-1">
                        <label
                            class="c-form-label"
                            for="voucher_input"
                        >Code eingeben</label>
                        <input
                            type="text"
                            id="voucher_input"
                            name="voucher"
                            class="c-form-input u-text-uppercase js-pf-voucher-input"
                            placeholder="GUT-XXXX-XXXX"
                            autocomplete="off"
                            value="<?php echo \htmlspecialchars($viewDto->voucherInput); ?>"
                        >
                    </div>
                    <button
                        type="submit"
                        class="c-button c-button--primary c-button--medium"
                    >Einlösen</button>
                </form>
            </div>
            <?php } ?>

            <!-- HAUPT-FORMULAR -->
            <form
                action="index"
                method="POST"
                id="permitForm"
                class="c-form"
            >
                <input
                    type="hidden"
                    name="csrf_token"
                    value="<?php echo $csrfToken; ?>"
                >
                <input
                    type="hidden"
                    name="voucher"
                    class="js-pf-voucher-input"
                    value="<?php echo \htmlspecialchars($viewDto->voucherInput); ?>"
                >

                <!-- SCHRITT 1: PERSÖNLICH -->
                <div class="c-form-section">
                    <h4 class="c-form-section__title"><span class="c-form-section__step">1</span> Ihre Daten</h4>
                    <div class="o-grid o-grid--2-1 u-gap-m">
                        <div class="c-form-group">
                            <label
                                class="c-form-label"
                                for="name"
                            >Vollständiger Name</label>
                            <input
                                type="text"
                                id="name"
                                name="name"
                                class="c-form-input"
                                required
                                placeholder="Vorname Nachname"
                                autocomplete="name"
                                value="<?php echo \htmlspecialchars($viewDto->name); ?>"
                                <?php echo $viewDto->nameReadonlyAttr; ?>
                            >
                        </div>
                        <div class="c-form-group">
                            <label
                                class="c-form-label"
                                for="parzelle"
                            >Parzelle</label>
                            <input
                                type="text"
                                name="parzelle"
                                id="parzelle"
                                class="c-form-input js-pf-parzelle"
                                required
                                placeholder="Nr."
                                autocomplete="off"
                                value="<?php echo \htmlspecialchars($viewDto->parzelle); ?>"
                                <?php echo $viewDto->parzelleReadonlyAttr; ?>
                            >
                        </div>
                    </div>
                    <div class="c-form-group u-margin-block-start-m">
                        <label
                            class="c-form-label"
                            for="email"
                        >E-Mail-Adresse</label>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            class="c-form-input"
                            required
                            placeholder="ihre@mail.de"
                            autocomplete="email"
                            value="<?php echo \htmlspecialchars($viewDto->email); ?>"
                            <?php echo $viewDto->emailReadonlyAttr; ?>
                        >
                        <small class="u-text-muted u-margin-block-start-xs">An diese Adresse senden wir die
                            Ausnahmegenehmigung.</small>
                    </div>
                </div>

                <!-- SCHRITT 2: FAHRZEUG -->
                <div class="c-form-section">
                    <h4 class="c-form-section__title"><span class="c-form-section__step">2</span> Fahrzeug & Zweck
                    </h4>
                    <div class="o-grid o-grid--2-cols u-gap-m">
                        <div class="c-form-group">
                            <label
                                class="c-form-label"
                                for="typ"
                            >Fahrzeug-Typ</label>
                            <select
                                name="typ"
                                id="typ"
                                class="c-form-select js-pf-typ"
                                autocomplete="off"
                                <?php echo $viewDto->typReadonlyAttr; ?>
                            >
                                <?php foreach ($viewDto->vehicleOptions as $opt) { ?>
                                <option
                                    value="<?php echo \htmlspecialchars($opt['value']); ?>"
                                    <?php echo $opt['selectedAttr']; ?>
                                >
                                    <?php echo \htmlspecialchars($opt['label']); ?>
                                </option>
                                <?php } ?>
                            </select>
                        </div>
                        <div class="c-form-group">
                            <label
                                class="c-form-label js-pf-label-kennzeichen"
                                id="label_kennzeichen"
                                for="kennzeichen"
                            >Amtl. Kennzeichen (B-ML 9876)</label>
                            <input
                                type="text"
                                name="kennzeichen"
                                id="kennzeichen"
                                class="c-form-input js-pf-kennzeichen"
                                required
                                maxlength="15"
                                pattern="[A-Za-z0-9\-\s]+"
                                title="Bitte geben Sie nur ein gültiges Kennzeichen ein."
                                placeholder="B-HD 123"
                                autocomplete="off"
                                value="<?php echo \htmlspecialchars($viewDto->kennzeichen); ?>"
                                <?php echo $viewDto->kennzeichenReadonlyAttr; ?>
                            >
                        </div>
                    </div>

                    <!-- Dynamisches Firmenfeld -->
                    <div
                        class="c-form-group u-margin-block-start-m js-pf-firma-wrapper"
                        id="firma_wrapper"
                        hidden
                    >
                        <label
                            class="c-form-label"
                            for="firma"
                        >Firma / Lieferant</label>
                        <input
                            type="text"
                            name="firma"
                            id="firma"
                            class="c-form-input js-pf-firma"
                            placeholder="Name der Firma"
                            autocomplete="organization"
                            value="<?php echo \htmlspecialchars($viewDto->firma); ?>"
                            <?php echo $viewDto->firmaReadonlyAttr; ?>
                        >
                    </div>

                    <div class="c-form-group u-margin-block-start-m">
                        <label
                            class="c-form-label"
                            for="zweck"
                        >Grund der Einfahrt</label>
                        <select
                            name="zweck"
                            id="zweck"
                            class="c-form-select"
                            autocomplete="off"
                            <?php echo $viewDto->zweckReadonlyAttr; ?>
                        >
                            <?php foreach ($viewDto->purposeOptions as $opt) { ?>
                            <option
                                value="<?php echo \htmlspecialchars($opt['value']); ?>"
                                <?php echo $opt['selectedAttr']; ?>
                            >
                                <?php echo \htmlspecialchars($opt['label']); ?>
                            </option>
                            <?php } ?>
                        </select>
                    </div>
                </div>

                <!-- SCHRITT 3: ZEITRAUM -->
                <div class="c-form-section">
                    <h4 class="c-form-section__title"><span class="c-form-section__step">3</span> Gültigkeit</h4>
                    <?php if ($viewDto->hasMultipleTemplates) { ?>
                    <!-- Mehrere Optionen: Auswahl anzeigen -->
                    <div class="c-form-group u-margin-block-end-m">
                        <label
                            class="c-form-label"
                            for="template_key"
                        >Gewünschte Dauer</label>
                        <select
                            name="template_key"
                            id="template_key"
                            class="c-form-select js-pf-template"
                            autocomplete="off"
                            <?php echo $viewDto->templateKeyReadonlyAttr; ?>
                        >
                            <?php foreach ($viewDto->templateOptions as $opt) { ?>
                            <option
                                value="<?php echo \htmlspecialchars($opt['value']); ?>"
                                <?php echo $opt['selectedAttr']; ?>
                            >
                                <?php echo \htmlspecialchars($opt['label']); ?>
                            </option>
                            <?php } ?>
                        </select>
                    </div>
                    <?php } else { ?>
                    <!-- Nur eine Option: Feld verstecken, Wert für JS/Backend bereitstellen -->
                    <input
                        type="hidden"
                        name="template_key"
                        id="template_key"
                        class="js-pf-template"
                        value="<?php echo \htmlspecialchars($viewDto->singleTemplateKey); ?>"
                    >
                    <?php } ?>

                    <div class="o-grid o-grid--2-cols u-gap-m">
                        <div class="c-form-group">
                            <label
                                class="c-form-label"
                                for="datum_von"
                            >Beginn ab</label>
                            <input
                                type="date"
                                name="datum_von"
                                id="datum_von"
                                class="c-form-input js-pf-von"
                                required
                                autocomplete="off"
                                value="<?php echo \htmlspecialchars($viewDto->datumVon); ?>"
                                <?php echo $viewDto->datumVonReadonlyAttr; ?>
                            >
                        </div>
                        <div class="c-form-group">
                            <label
                                class="c-form-label"
                                for="datum_bis"
                            >Gültig bis</label>
                            <input
                                type="date"
                                name="datum_bis"
                                id="datum_bis"
                                class="c-form-input js-pf-bis"
                                required
                                autocomplete="off"
                                value="<?php echo \htmlspecialchars($viewDto->datumBis); ?>"
                                <?php echo $viewDto->datumBisReadonlyAttr; ?>
                            >
                        </div>
                    </div>

                    <div
                        id="dynamic-date-info"
                        class="c-box c-box--highlight u-margin-block-start-l u-text-sm js-pf-dynamic-date-info"
                        hidden
                    >
                        <div
                            id="dynamic-opening-hours"
                            class="u-margin-block-end-s js-pf-dynamic-opening"
                        ></div>
                        <div
                            id="dynamic-holiday-notice"
                            class="c-box c-box--danger-soft js-pf-dynamic-holiday"
                            hidden
                        ></div>
                    </div>
                </div>

                <!-- Preis-Anzeige mit Rabatt-Logik -->
                <div
                    id="price-display"
                    class="c-price-display u-margin-block-start-m u-margin-block-end-l js-pf-price-display"
                >
                    Wird berechnet...
                </div>

                <?php if (!empty($viewDto->agreements)) { ?>
                <div class="c-form-group c-box c-box--subtle u-margin-block-end-l">
                    <p class="u-margin-block-start-none u-margin-block-end-s u-font-bold u-text-muted">
                        Rechtliches:</p>
                    <div class="u-flex u-flex-column u-gap-s">
                        <?php foreach ($viewDto->agreements as $key => $agree) { ?>
                        <label class="c-checkbox">
                            <input
                                type="checkbox"
                                name="agreements[<?php echo $key; ?>]"
                                value="1"
                                id="agree_<?php echo $key; ?>"
                                class="c-checkbox__input"
                                <?php echo $agree['requiredAttr']; ?>
                                <?php echo $agree['checkedAttr']; ?>
                            >
                            <span class="c-checkbox__label">
                                <?php echo $agree['label_html']; ?>
                                <?php if ($agree['required']) { ?>
                                <span
                                    class="c-checkbox__required"
                                    title="Pflichtfeld"
                                    aria-hidden="true"
                                >*</span>
                                <?php } ?>
                            </span>
                        </label>
                        <?php } ?>
                    </div>
                </div>
                <?php } ?>

                <?php /* Serverseitig gerenderter Honeypot fängt auch Scraper ohne JS-Fähigkeiten */ ?>
                <div
                    class="u-visually-hidden"
                    aria-hidden="true"
                >
                    <label for="hp_contact_website">Bitte lassen Sie dieses Feld leer, wenn Sie ein Mensch
                        sind.</label>
                    <input
                        type="text"
                        name="hp_contact_website"
                        id="hp_contact_website"
                        tabindex="-1"
                        autocomplete="off"
                    >
                </div>

                <button
                    type="submit"
                    class="c-button c-button--primary c-button--submit u-inline-size-100 u-margin-block-start-l"
                >
                    <?php echo \htmlspecialchars($viewDto->submitButtonText); ?>
                </button>

                <?php if (!$viewDto->hasActiveVoucher) { ?>
                <p class="u-text-sm u-text-center u-text-muted u-margin-block-start-s">Die Auswahl der
                    Zahlungsart erfolgt erst nach erfolgreicher E-Mail-Bestätigung.</p>
                <div class="u-margin-block-start-m u-text-center">
                    <a
                        href="verify"
                        class="u-text-sm u-color-primary u-font-semibold"
                    > Ich habe bereits einen Bestätigungs-Code erhalten </a>
                </div>
                <br>
                <?php } ?>
            </form>
            <?php } ?>
        </div>
    </div>
</div>

<script nonce="<?php echo $cspNonce; ?>">
window.KGA_TEMPLATES = <?php echo $viewDto->tplMetadataJson; ?>;

</script>
