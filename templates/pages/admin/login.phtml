<?php

use App\Contracts\System\AssetHelperInterface;

/**
 * Page: Admin Login
 * VSA FIX: PHTML ist 100% dumm. Kein $_SESSION-Zugriff und keine Fallbacks mehr, nutzt injiziertes `$savedUser`, `$csrfToken` und `$redirectCode`.
 *
 * Template für die Admin-Login-Maske.
 * Zeigt das Anmeldeformular für Systembetreuer und Administratoren an.
 *
 * @var AssetHelperInterface $asset
 * @var array<string, mixed> $settings
 * @var string $vereinsName
 * @var string $savedUser
 * @var string $appRoot
 * @var string $cspNonce
 * @var string $csrfToken
 * @var string $redirectCode
 */
$layout = 'public';
$pageTitle = 'Admin Login - ' . $vereinsName;
?>

<div class="c-tab-content-container o-container o-container--small u-padding-block-xl">
    <div class="c-card c-card--primary u-margin-inline-auto">
        <header class="c-card__header u-text-center">
            <img
                src="<?php echo $asset->url('assets/img/icons/user.webp'); ?>"
                class="c-icon c-icon--md u-opacity-50"
                loading="lazy"
                alt=""
            >
            <h2 class="u-margin-none u-margin-block-start-s">Vorstands-Login</h2>
            <p class="u-color-muted u-margin-block-start-s">
                Bitte loggen Sie sich ein, um den Verwaltungsbereich von
                <?php echo \htmlspecialchars($vereinsName); ?> zu betreten.
            </p>
        </header>

        <div class="c-card__body">
            <?php require $appRoot . '/templates/partials/frontend/alerts.phtml'; ?>

            <form
                method="POST"
                id="adminLoginForm"
                action="<?php echo $settings['base_url']; ?>admin_login"
                class="c-form"
            >
                <input
                    type="hidden"
                    name="csrf_token"
                    value="<?php echo $csrfToken; ?>"
                >

                <div class="c-form-group">
                    <label
                        class="c-form-label"
                        for="login_user"
                    >Benutzername</label>
                    <input
                        type="text"
                        id="login_user"
                        name="user"
                        class="c-form-input"
                        placeholder="z.B. vorstand"
                        value="<?php echo \htmlspecialchars($savedUser); ?>"
                        required
                        autofocus
                        autocomplete="username"
                    >
                </div>

                <div class="c-form-group u-margin-block-start-m">
                    <label
                        class="c-form-label"
                        for="login_pass"
                    >Passwort</label>
                    <div class="js-password-toggle c-password-toggle">
                        <input
                            type="password"
                            name="pass"
                            id="login_pass"
                            class="c-form-input c-password-toggle__input"
                            placeholder="••••••••"
                            value=""
                            required
                            autocomplete="current-password"
                        >
                        <button
                            type="button"
                            class="c-password-toggle__btn"
                            aria-label="Passwort im Klartext anzeigen"
                            aria-pressed="false"
                        >
                            <img
                                src="<?php echo $asset->url('assets/img/icons/view.webp'); ?>"
                                class="c-icon"
                                loading="lazy"
                                alt=""
                            >
                        </button>
                    </div>
                </div>

                <input
                    type="hidden"
                    name="action"
                    value="login"
                >
                <input
                    type="hidden"
                    name="code"
                    value="<?php echo \htmlspecialchars($redirectCode, \ENT_QUOTES, 'UTF-8'); ?>"
                >

                <button
                    type="submit"
                    class="c-button c-button--primary c-button--submit u-inline-size-100 u-margin-block-start-l"
                >
                    Sicher einloggen
                    <img
                        src="<?php echo $asset->url('assets/img/icons/shield.webp'); ?>"
                        class="c-icon c-button__icon"
                        loading="lazy"
                        alt=""
                    >
                </button>
            </form>
        </div>
    </div>
</div>
