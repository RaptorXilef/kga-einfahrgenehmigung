<?php

use App\Contracts\Storage\UserRepositoryInterface;
use App\Contracts\System\AssetHelperInterface;
use App\Contracts\System\ImageStorageInterface;
use App\Core\Service\AuthService;

/**
 * Page: User Profile
 *
 * Verwaltung von Avatar, Login-Name und Passwort für den angemeldeten User.
 *
 * Profil-Einstellungsseite des aktuell angemeldeten Administrators.
 * Erlaubt die Personalisierung des eigenen Kontos (Anzeigename, Passwort-Änderung, Avatar-WebP-Upload).
 *
 * @var AssetHelperInterface $asset
 * @var AuthService $auth
 * @var ImageStorageInterface $imageStorage
 * @var UserRepositoryInterface $userRepository
 * @var array $settings
 * @var string $appRoot
 * @var string $cspNonce
 * @var string $role     Der Klartext-Name der eigenen Rolle.
 * @var string $userId   Die aktive Benutzer-ID.
 * @var string $username Der aktuelle Anzeigename.
 */
$layout = 'admin';
$pageTitle = 'Mein Profil - ' . ($settings['vereins_name'] ?? '');
$userImage = $imageStorage->getImageUrl('user_images', $userId, 'icon-user-default.webp');
?>

<main class="o-container o-container--large u-padding-y-l">
    <div class="u-margin-auto">
        <header class="u-flex u-justify-between u-align-end u-margin-bottom-l">
            <div>
                <h1 class="u-margin-none u-text-left">
                    <img src="<?php echo $asset->url('assets/img/icons/icon-user.webp'); ?>"
                         class="c-icon c-icon--md"
                         alt="">
                    Mein Profil
                </h1>
                <p class="u-color-muted">Verwalten Sie Ihre persönlichen Einstellungen und Sicherheit.</p>
            </div>
            <a href="admin" class="c-button c-button--secondary">← Zum Dashboard</a>
        </header>

        <?php require $appRoot . '/templates/partials/frontend/alerts.phtml'; ?>

        <div class="o-grid o-grid--auto-fit-lg u-gap-l u-align-start">
            <aside class="u-flex-grow-1">
                <section
                         class="c-card c-dropzone c-dropzone--preview js-avatar-dropzone js-preview-only u-text-center u-padding-around-l">
                    <div class="c-avatar-dropzone c-avatar-dropzone--large u-margin-auto u-margin-bottom-m u-position-relative">
                        <img src="<?php echo $userImage; ?>"
                             class="c-avatar c-avatar--xl js-preview-img"
                             alt="">
                        <div class="c-dropzone__overlay u-border-radius-circle">Foto ablegen</div>
                    </div>
                    <form method="POST"
                          action="<?php echo $settings['base_url']; ?>change_own_avatar"
                          enctype="multipart/form-data">
                        <input type="hidden"
                               name="csrf_token"
                               value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                        <input type="file"
                               name="avatar"
                               id="avatar-input"
                               class="u-hidden"
                               accept="image/*">
                        <button type="button"
                                class="c-button c-button--primary u-width-100 js-trigger-click"
                                data-target="avatar-input">
                            <img src="<?php echo $asset->url('assets/img/icons/icon-camera.webp'); ?>"
                                 class="c-icon"
                                 alt=""> Bild ändern
                        </button>
                    </form>
                    <div class="u-margin-top-l u-padding-top-m u-border-top">
                        <small class="u-color-muted u-font-bold u-text-uppercase"> Rolle </small>
                        <div class="u-margin-top-xs">
                            <span class="c-badge c-badge--primary">
                                <?php echo \strtoupper(\htmlspecialchars($role)); ?>
                            </span>
                        </div>
                        <code class="u-color-muted u-display-block u-margin-top-s u-text-xs">
                            ID: <?php echo \htmlspecialchars((string) $userId, \ENT_QUOTES, 'UTF-8'); ?>
                        </code>
                    </div>
                </section>
            </aside>

            <div class="u-flex u-flex-column u-gap-l u-flex-grow-1">
                <section class="c-card">
                    <header class="c-card__header">
                        <h3 class="u-margin-none">Anmeldedaten</h3>
                    </header>
                    <div class="c-card__body">
                        <form method="POST"
                              action="<?php echo $settings['base_url']; ?>change_own_username"
                              class="o-grid o-grid--auto-right u-gap-m">
                            <input type="hidden"
                                   name="csrf_token"
                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                            <div class="c-form-group">
                                <label class="c-form-label"
                                       for="profile_username">Anzeigename / Login</label>
                                <input type="text"
                                       id="profile_username"
                                       name="new_username"
                                       class="c-form-input"
                                       value="<?php echo \htmlspecialchars($username); ?>"
                                       required
                                       autocomplete="username">
                            </div>
                            <div class="u-flex u-align-end">
                                <button type="submit"
                                        class="c-button c-button--primary c-button--submit">Speichern</button>
                            </div>
                        </form>
                    </div>
                </section>

                <section class="c-card c-card--danger">
                    <header class="c-card__header">
                        <h3 class="u-margin-none">Sicherheit & Passwort</h3>
                    </header>
                    <div class="c-card__body">
                        <form method="POST"
                              action="<?php echo $settings['base_url']; ?>change_own_password"
                              class="u-flex u-flex-column u-gap-m">
                            <input type="hidden"
                                   name="csrf_token"
                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                            <div class="c-form-group">
                                <label class="c-form-label"
                                       for="old_password">Aktuelles Passwort</label>
                                <input type="password"
                                       id="old_password"
                                       name="old_password"
                                       class="c-form-input"
                                       required
                                       placeholder="••••••••"
                                       autocomplete="current-password">
                            </div>
                            <hr class="u-border-top u-width-100 u-margin-y-none"
                                style="border: none;">
                            <div class="o-grid o-grid--2-cols u-gap-m">
                                <div class="c-form-group">
                                    <label class="c-form-label"
                                           for="new_password">Neues Passwort</label>
                                    <input type="password"
                                           id="new_password"
                                           name="new_password"
                                           class="c-form-input"
                                           required
                                           minlength="8"
                                           placeholder="Mind. 8 Zeichen"
                                           autocomplete="new-password">
                                </div>
                                <div class="c-form-group">
                                    <label class="c-form-label"
                                           for="confirm_password">Wiederholen</label>
                                    <input type="password"
                                           id="confirm_password"
                                           name="confirm_password"
                                           class="c-form-input"
                                           required
                                           minlength="8"
                                           placeholder="Bestätigen"
                                           autocomplete="new-password">
                                </div>
                            </div>
                            <button type="submit"
                                    class="c-button c-button--danger u-margin-top-s c-button--submit">
                                Passwort sicher aktualisieren
                            </button>
                        </form>
                    </div>
                </section>
            </div>
        </div>
    </div>
</main>
