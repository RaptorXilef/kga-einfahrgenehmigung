<?php

/**
 * @var \App\Core\Entity\Permit[] $permits
 * @var string                    $email
 * @var array                     $settings
 * @var int                       $currentArchiveYear
 */
?>
<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="UTF-8">
    <title>Meine Genehmigungen - <?php echo \htmlspecialchars($settings['vereins_name']); ?></title>
    <link rel="stylesheet" href="assets/css/main.min.css">
</head>

<body class="u-bg-light">
    <div class="c-container u-padding-y-l">
        <header class="u-flex u-justify-between u-align-center u-margin-bottom-l">
            <div>
                <h1>Meine Genehmigungen</h1>
                <p class="u-text-muted">Angemeldet als: <strong><?php echo \htmlspecialchars($email); ?></strong></p>
            </div>
            <a href="history.php?action=logout" class="c-button c-button--text">Abmelden</a>
        </header>

        <section class="c-card">
            <?php if (empty($permits)) { ?>
                <p class="u-padding-around-m">Keine abgeschlossenen Genehmigungen gefunden.</p>
            <?php } else { ?>
                <table class="c-table">
                    <thead>
                        <tr>
                            <th>SmartCode</th>
                            <th>Zeitraum</th>
                            <th>Parzelle</th>
                            <th>Status</th>
                            <th class="u-text-right">Aktion</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($permits as $p) { ?>
                            <tr>
                                <td><code><?php echo $p->code; ?></code></td>
                                <td><small><?php echo $p->von->format('d.m.Y'); ?> - <?php echo $p->bis->format('d.m.Y'); ?></small></td>
                                <td>Nr. <?php echo $p->parzelle; ?></td>
                                <td>
                                    <span class="c-badge c-badge--<?php echo $p->status === 'bezahlt' ? 'success' : 'warning'; ?>">
                                        <?php echo \strtoupper($p->status); ?>
                                    </span>
                                </td>
                                <td class="u-text-right">
                                    <div class="u-flex u-gap-xs u-justify-end">
                                        <a href="history.php?action=print&code=<?php echo $p->code; ?>" class="c-button c-button--primary c-button--small">
                                            🖨 Dokument laden
                                        </a>
                                        <a href="check.php?code=<?php echo $p->code; ?>" class="c-button c-button--secondary c-button--small" title="Status prüfen">
                                            👁
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        <?php } ?>
                    </tbody>
                </table>
            <?php } ?>

            <div class="u-padding-around-m u-border-top u-text-center">
                <?php
                $lastYear = (int) \date('Y') - 1;
if ($currentArchiveYear !== $lastYear) {
    ?>
                    <a href="?load_archive=<?php echo $lastYear; ?>" class="c-button c-button--text">
                        📥 Daten aus dem Jahr <?php echo $lastYear; ?> laden
                    </a>
                <?php } else { ?>
                    <p class="u-text-small u-text-muted">Archiv <?php echo $lastYear; ?> ist geladen.</p>
                <?php } ?>
            </div>
        </section>

        <div class="u-margin-top-l u-text-center">
            <a href="index.php" class="c-button">Neuen Antrag stellen</a>
        </div>
    </div>
</body>

</html>
