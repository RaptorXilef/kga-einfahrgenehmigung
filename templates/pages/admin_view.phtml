<!DOCTYPE html>
<html lang="de" data-theme="light">
<head>
    <meta charset="UTF-8">
    <title>Admin - <?= htmlspecialchars($settings['vereins_name']) ?></title>
    <link rel="stylesheet" href="assets/css/main.min.css">
</head>
<body class="l-admin">
    <main class="c-container">
        <header class="c-page-header">
            <h1>Verwaltung Einfahrgenehmigungen</h1>
        </header>

        <?php if ($message): ?>
            <div class="c-alert c-alert--info"><?= htmlspecialchars($message) ?></div>
        <?php endif; ?>

        <section class="c-card u-margin-bottom">
            <div class="c-card__header">
                <h2>Daten-Migration</h2>
            </div>
            <div class="c-card__body">
                <form method="POST" class="c-form--inline">
                    <select name="direction" class="c-form-group__input">
                        <option value="to_mysql">JSON ➔ MySQL (Export)</option>
                        <option value="to_json">MySQL ➔ JSON (Backup)</option>
                    </select>
                    <button type="submit" name="migrate" class="c-button c-button--secondary">Migration starten</button>
                </form>
            </div>
        </section>

        <section class="c-card">
            <table class="c-table">
                <thead>
                    <tr>
                        <th>Code</th>
                        <th>Name</th>
                        <th>Kennzeichen</th>
                        <th>Status</th>
                        <th>Aktion</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($storage->getAll() as $p): ?>
                    <tr>
                        <td><code><?= $p->code ?></code></td>
                        <td><?= htmlspecialchars($p->name) ?></td>
                        <td><?= htmlspecialchars($p->kennzeichen) ?></td>
                        <td>
                            <span class="c-badge c-badge--<?= $p->status === 'bezahlt' ? 'success' : 'warning' ?>">
                                <?= $p->status ?>
                            </span>
                        </td>
                        <td>
                            <?php if ($p->status !== 'bezahlt'): ?>
                                <a href="?code=<?= $p->code ?>&token=<?= hash('sha256', $p->code . $settings['geheimnis']) ?>"
                                   class="c-button c-button--small">Freischalten</a>
                            <?php endif; ?>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </section>
    </main>
</body>
</html>
