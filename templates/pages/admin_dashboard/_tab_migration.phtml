<?php

/**
 * Partial: Dashboard Tab - Data Migration
 *
 * SQL/JSON Synchronisation und Backup-Wiederherstellung.
 * Modernisiertes Kachel-Design.
 *
 * Path: templates/pages/admin_dashboard/_tab_migration.phtml
 *
 * SPDX-License-Identifier: LicenseRef-Proprietary
 * Copyright (c) 2026 Felix Maywald alias RaptorXilef. All rights reserved.
 * Usage without explicit permission is strictly prohibited.
 * See LICENSE.md for full license details.
 *
 * @var \App\Core\Service\AuthService                 $auth
 * @var \App\Infrastructure\Maintenance\BackupService $backupService
 * @var array                                         $settings
 */
if ($auth->hasPermission('dashboard.migration.view')) {
    $targets = [
        'groups'               => ['label' => 'Benutzergruppen', 'icon' => '🛡️'],
        'magic_links'          => ['label' => 'Login-Tokens', 'icon' => '🔗'],
        'mail_log'             => ['label' => 'E-Mail Protokolle', 'icon' => '📧'],
        'mail_queue'           => ['label' => 'Mail-Warteschlange', 'icon' => '📤'],
        'pending_verification' => ['label' => 'Warteraum (E-Mail)', 'icon' => '✉️'],
        'permits'              => ['label' => 'Genehmigungen', 'icon' => '📄'],
        'permits_archive'      => ['label' => 'Genehmigungs-Archiv', 'icon' => '🗄️'],
        'users'                => ['label' => 'Benutzerkonten', 'icon' => '👥'],
        'verified_pending'     => ['label' => 'Warteraum (Zahlung)', 'icon' => '💳'],
        'vouchers'             => ['label' => 'Gutscheine', 'icon' => '🎟️'],
        'vouchers_archive'     => ['label' => 'Gutschein-Archiv', 'icon' => '📚'],
        'login_attempts'       => ['label' => 'Login-Versuche', 'icon' => '🛡'],
        'update_migrations'    => ['label' => 'Update-Migration-Verlauf', 'icon' => '♻'],
    ];
    ?>
    <div class="c-tab-content-container">

        <?php if ($auth->hasPermission('dashboard.migration.sync.view')) { ?>
            <div class="u-margin-bottom-l">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <img src="<?php echo $settings['base_url']; ?>assets/img/icons/icon-sync.webp"
                         class="c-icon u-font-xl"
                         alt="Sync">
                    <div>
                        <h3 class="u-margin-none">Daten-Synchronisation</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Verschieben oder synchronisieren Sie Daten
                            zwischen JSON-Dateien und MySQL.</p>
                    </div>
                </header>

                <div class="c-alert c-alert--warning u-margin-bottom-m">
                    <img src="<?php echo $settings['base_url']; ?>assets/img/icons/status-warning.webp"
                         class="c-icon"
                         alt="">
                    <span><strong>Achtung:</strong> Migrationen überschreiben Daten am Zielort. Es wird automatisch ein Backup erstellt.</span>
                </div>

                <div class="u-grid"
                     style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">
                    <?php foreach ($targets as $key => $data) { ?>
                        <div class="c-card"
                             style="margin-bottom: 0; border-top: 4px solid var(--primary-color); display: flex; flex-direction: column;">
                            <div class="c-card__header u-flex u-align-center u-gap-s"
                                 style="padding: 1rem 1.5rem;">
                                <span style="font-size: 1.5rem;"><?php echo $data['icon']; ?></span>
                                <h4 class="u-margin-none"
                                    style="font-size: 1.1rem;"><?php echo $data['label']; ?></h4>
                            </div>

                            <div class="c-card__body u-flex u-flex-column u-gap-m"
                                 style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                                <div class="u-flex u-gap-s u-width-100">
                                    <form method="POST"
                                          onsubmit="return confirm('Daten nach MySQL migrieren?')"
                                          class="u-width-100">
                                        <input type="hidden"
                                               name="csrf_token"
                                               value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                        <input type="hidden"
                                               name="action"
                                               value="migrate_data">
                                        <input type="hidden"
                                               name="target"
                                               value="<?php echo $key; ?>">
                                        <input type="hidden"
                                               name="direction"
                                               value="json_to_mysql">
                                        <button type="submit"
                                                class="c-button c-button--secondary u-width-100"
                                                style="font-size: 0.85rem;"
                                                <?php echo ! $auth->hasPermission("dashboard.migration.{$key}.json_to_mysql") ? 'disabled title="Keine Berechtigung"' : ''; ?>>
                                            JSON ➔ SQL
                                        </button>
                                    </form>

                                    <form method="POST"
                                          onsubmit="return confirm('Daten nach JSON migrieren?')"
                                          class="u-width-100">
                                        <input type="hidden"
                                               name="csrf_token"
                                               value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                        <input type="hidden"
                                               name="action"
                                               value="migrate_data">
                                        <input type="hidden"
                                               name="target"
                                               value="<?php echo $key; ?>">
                                        <input type="hidden"
                                               name="direction"
                                               value="mysql_to_json">
                                        <button type="submit"
                                                class="c-button c-button--secondary u-width-100"
                                                style="font-size: 0.85rem;"
                                                <?php echo ! $auth->hasPermission("dashboard.migration.{$key}.mysql_to_json") ? 'disabled title="Keine Berechtigung"' : ''; ?>>
                                            SQL ➔ JSON
                                        </button>
                                    </form>
                                </div>

                                <form method="POST"
                                      class="u-width-100 u-margin-top-auto">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="action"
                                           value="migrate_data">
                                    <input type="hidden"
                                           name="target"
                                           value="<?php echo $key; ?>">
                                    <input type="hidden"
                                           name="direction"
                                           value="sync">
                                    <button type="submit"
                                            class="c-button c-button--primary u-width-100"
                                            style="font-size: 0.9rem; font-weight: 600;"
                                            <?php echo ! $auth->hasPermission("dashboard.migration.{$key}.sync") ? 'disabled title="Keine Berechtigung"' : ''; ?>>
                                        <img src="<?php echo $settings['base_url']; ?>assets/img/icons/icon-sync.webp"
                                             class="c-icon"
                                             alt="Sync">
                                        </a>
                                        &ensp;Zusammenführen (Sync)&ensp;
                                        <img src="<?php echo $settings['base_url']; ?>assets/img/icons/status-warning.webp"
                                             class="c-icon"
                                             alt="Gefährliche Aktion!">
                                        </a>
                                    </button>
                                </form>
                            </div>
                        </div>
                    <?php } ?>
                </div>
            </div>
        <?php } ?>

        <!-- Löschen -->
        <?php if ($auth->hasPermission('dashboard.admin-tools.view')) { ?>
            <div class="u-margin-top-xl">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">🛠️</span>
                    <div>
                        <h3 class="u-margin-none">Gefahrenzone: System-Werkzeuge</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Cache leeren und Datenbestände zurücksetzen.
                        </p>
                    </div>
                </header>

                <div class="u-grid"
                     style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">

                    <?php if ($auth->hasPermission('dashboard.migration.delete-cache.execute')) { ?>
                        <div class="c-card"
                             style="margin-bottom: 0; border-top: 4px solid var(--danger-color); display: flex; flex-direction: column;">
                            <div class="c-card__header u-flex u-align-center u-gap-s"
                                 style="padding: 1rem 1.5rem;">
                                <h4 class="u-margin-none"
                                    style="font-size: 1.1rem;">🧹 System-Cache</h4>
                            </div>
                            <div class="c-card__body u-flex u-flex-column u-gap-m"
                                 style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                                <p class="u-text-small u-margin-none">Löscht temporäre Dateien (wie z.B. Deptrac Caches
                                    oder kompilierte Rechte). Behebt oft Anzeige-Bugs nach Updates.</p>
                                <form method="POST"
                                      onsubmit="return confirm('Cache jetzt leeren?')"
                                      class="u-margin-top-auto">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="action"
                                           value="clear_cache">
                                    <button type="submit"
                                            class="c-button c-button--secondary u-width-100"
                                            style="font-size: 0.9rem;">
                                        Cache leeren
                                    </button>
                                </form>
                            </div>
                        </div>
                    <?php } ?>

                    <?php if ($auth->hasPermission('dashboard.migration.delete-data.execute')) { ?>
                        <div class="c-card"
                             style="margin-bottom: 0; border-top: 4px solid var(--danger-color); display: flex; flex-direction: column;">
                            <div class="c-card__header u-flex u-align-center u-gap-s"
                                 style="padding: 1rem 1.5rem;">
                                <h4 class="u-margin-none"
                                    style="font-size: 1.1rem;">🗑️ Datenbestand löschen</h4>
                            </div>
                            <div class="c-card__body u-flex u-flex-column u-gap-m"
                                 style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                                <p class="u-text-small u-margin-none">Leert eine komplette Tabelle oder JSON-Datei. Es
                                    wird automatisch ein Sicherungs-Backup erstellt.</p>
                                <form method="POST"
                                      onsubmit="return confirm('WARNUNG: Die gewählten Daten werden unwiderruflich GELÖSCHT! Fortfahren?')"
                                      class="u-margin-top-auto u-flex u-flex-column u-gap-s">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="action"
                                           value="truncate_target">

                                    <select name="target"
                                            class="c-form-select"
                                            required>
                                        <option value="">-- Bereich wählen --</option>
                                        <?php foreach ($targets as $key => $data) { ?>
                                            <option value="<?php echo $key; ?>"><?php echo $data['label']; ?></option>
                                        <?php } ?>
                                    </select>

                                    <div class="u-flex u-gap-s">
                                        <select name="engine"
                                                class="c-form-select"
                                                required>
                                            <option value="all">Aus JSON & MySQL löschen</option>
                                            <option value="json">NUR aus JSON löschen</option>
                                            <option value="mysql">NUR aus MySQL löschen</option>
                                        </select>
                                        <button type="submit"
                                                class="c-button c-button--danger"
                                                style="width: auto; padding: 0 16px;">
                                            Löschen
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    <?php } ?>
                    <?php if ($auth->hasPermission('dashboard.migration.anonymize.execute')) { ?>
                        <div class="c-card"
                             style="margin-bottom: 0; border-top: 4px solid var(--info-color); display: flex; flex-direction: column;">
                            <div class="c-card__header u-flex u-align-center u-gap-s"
                                 style="padding: 1rem 1.5rem;">
                                <h4 class="u-margin-none"
                                    style="font-size: 1.1rem;">🛡️ DSGVO-Anonymisierung</h4>
                            </div>
                            <div class="c-card__body u-flex u-flex-column u-gap-m"
                                 style="padding: 1.25rem 1.5rem; flex-grow: 1;">
                                <p class="u-text-small u-margin-none">Anonymisiert personenbezogene Daten in
                                    Archiv-Einträgen, die älter als 10 Jahre sind (Aufbewahrungsfrist).</p>
                                <form method="POST"
                                      onsubmit="return confirm('Sollen alte Archiv-Einträge jetzt DSGVO-konform anonymisiert werden?')"
                                      class="u-margin-top-auto">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                    <input type="hidden"
                                           name="action"
                                           value="anonymize_archive">
                                    <button type="submit"
                                            class="c-button c-button--secondary u-width-100"
                                            style="font-size: 0.9rem;">
                                        Jetzt prüfen & bereinigen
                                    </button>
                                </form>
                            </div>
                        </div>
                    <?php } ?>

                </div>
            </div>
        <?php } ?>

        <!-- Backups -->
        <?php if ($auth->hasPermission('dashboard.migration.backups.view')) { ?>
            <div class="u-margin-top-xl">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">💾</span>
                    <div>
                        <h3 class="u-margin-none">Verfügbare Backups</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Übersicht aller Systemsicherungen zur
                            Wiederherstellung.</p>
                    </div>
                </header>

                <?php if (empty($backups)) { ?>
                    <div class="c-card"
                         style="border-top: 4px solid var(--json-color);">
                        <div class="c-card__body u-padding-around-l u-text-center u-text-muted">
                            <span style="font-size: 2rem; display: block; margin-bottom: 10px;">📦</span>
                            Es sind noch keine Backups im System vorhanden.
                        </div>
                    </div>
                <?php } else { ?>
                    <div class="u-grid"
                         style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">
                        <?php foreach ($backups as $ts => $files) {
                            $dateObj       = \DateTimeImmutable::createFromFormat('Ymd-His', $ts);
                            $formattedDate = $dateObj ? $dateObj->format('d.m.Y — H:i:s') : $ts;
                            ?>
                            <div class="c-card"
                                 style="margin-bottom: 0; border-top: 4px solid var(--json-color); display: flex; flex-direction: column;">
                                <div class="c-card__header"
                                     style="padding: 1rem 1.5rem;">
                                    <h4 class="u-margin-none"
                                        style="font-size: 1.1rem;"><?php echo $formattedDate; ?></h4>
                                    <span class="u-text-muted u-text-small"><?php echo $ts; ?></span>
                                </div>

                                <div class="c-card__body u-flex u-flex-column u-gap-m"
                                     style="padding: 1.25rem 1.5rem; flex-grow: 1;">

                                    <div class="u-flex u-flex-wrap u-gap-xs">
                                        <?php foreach ($files as $file) {
                                            $badgeColor = \str_contains($file, 'sql.json') ? 'var(--primary-color)' : 'var(--json-color)';
                                            $cleanName  = \str_replace(['_file.json', '_sql.json'], '', $file);
                                            ?>
                                            <span class="c-badge" style="background: <?php echo $badgeColor; ?>; font-size: 0.7rem;">
                                                <?php echo \htmlspecialchars($cleanName); ?>
                                            </span>
                                        <?php } ?>
                                    </div>

                                    <?php if ($auth->hasPermission('dashboard.migration.restore.execute')) { ?>
                                        <form method="POST"
                                              onsubmit="return confirm('ACHTUNG: Dies überschreibt Ihre aktuellen Daten am Zielort mit dem alten Backup-Stand! Fortfahren?')"
                                              class="u-flex u-flex-column u-gap-s u-margin-top-auto">

                                            <input type="hidden"
                                                   name="csrf_token"
                                                   value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                            <input type="hidden"
                                                   name="action"
                                                   value="restore_data">
                                            <input type="hidden"
                                                   name="timestamp"
                                                   value="<?php echo $ts; ?>">

                                            <select name="target"
                                                    class="c-form-select"
                                                    required>
                                                <option value="">-- Bereich --</option>
                                                <?php foreach ($targets as $key => $data) { ?>
                                                    <option value="<?php echo $key; ?>"><?php echo $data['label']; ?></option>
                                                <?php } ?>
                                            </select>

                                            <div class="u-flex u-gap-s">
                                                <select name="engine"
                                                        class="c-form-select"
                                                        required>
                                                    <option value="all">In JSON & SQL</option>
                                                    <option value="json">Nur JSON</option>
                                                    <option value="mysql">Nur SQL</option>
                                                </select>

                                                <button type="submit"
                                                        class="c-button c-button--danger"
                                                        style="width: auto; padding: 0 16px; font-weight: 600;">
                                                    Restore
                                                </button>
                                            </div>
                                        </form>
                                    <?php } else { ?>
                                        <div class="u-margin-top-auto u-text-right">
                                            <span class="u-text-muted u-text-small">🚫 Keine Restore-Rechte</span>
                                        </div>
                                    <?php } ?>
                                </div>
                            </div>
                        <?php } ?>
                    </div>
                <?php } ?>
            </div>
        <?php } ?>

    </div>
<?php } ?>
