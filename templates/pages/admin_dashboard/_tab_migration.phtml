<?php

/**
 * Partial: Dashboard Tab - Data Migration
 *
 * SQL/JSON Synchronisation und Backup-Wiederherstellung.
 * Modernisiertes Kachel-Design.
 *
 * Path: templates/pages/admin_dashboard/_tab_migration.phtml
 *
 * SPDX-License-Identifier: LicenseRef-Proprietary
 * Copyright (c) 2026 Felix Maywald alias RaptorXilef. All rights reserved.
 * Usage without explicit permission is strictly prohibited.
 * See LICENSE.md for full license details.
 *
 * @var \App\Infrastructure\Auth\AuthService $auth
 * @var \App\Core\Service\BackupService      $backupService
 * @var array                                $settings
 */
if ($auth->hasPermission('dashboard.migration.view')) {
    $targets = [
        'users'                => ['label' => 'Benutzerkonten', 'icon' => '👥'],
        'vouchers'             => ['label' => 'Gutscheine', 'icon' => '🎟️'],
        'permits'              => ['label' => 'Genehmigungen', 'icon' => '📄'],
        'mail_log'             => ['label' => 'E-Mail Protokolle', 'icon' => '📧'],
        'pending_verification' => ['label' => 'Warteraum (E-Mail)', 'icon' => '⏳'],
        'magic_links'          => ['label' => 'Login-Tokens', 'icon' => '🔗'],
    ];

    // Achtung: Nutzt jetzt den neuen BackupService!
    // Falls du die Variable im Controller noch nicht angepasst hast, musst du sie als $backupService übergeben.
    $backups = $backupService->listBackups();
    ?>
    <div class="c-tab-content-container">

        <?php if ($auth->hasPermission('dashboard.migration.sync.view')) { ?>
            <div class="u-margin-bottom-l">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <img src="<?php echo $settings['base_url']; ?>assets/img/icons/icon-sync.webp"
                         class="c-icon u-font-xl"
                         alt="Sync">
                    <div>
                        <h3 class="u-margin-none">Daten-Synchronisation</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Verschieben oder synchronisieren Sie Daten
                            zwischen JSON-Dateien und MySQL.</p>
                    </div>
                </header>

                <div class="c-alert c-alert--warning u-margin-bottom-m">
                    <img src="<?php echo $settings['base_url']; ?>assets/img/icons/status-warning.webp"
                         class="c-icon"
                         alt="">
                    <span><strong>Achtung:</strong> Migrationen überschreiben Daten am Zielort. Es wird automatisch ein Backup erstellt.</span>
                </div>

                <div class="u-grid"
                     style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">
                    <?php foreach ($targets as $key => $data) { ?>
                        <div class="c-card"
                             style="margin-bottom: 0; border-top: 4px solid var(--primary-color);">
                            <div class="c-card__header u-flex u-align-center u-gap-s"
                                 style="padding: 1rem 1.5rem;">
                                <span style="font-size: 1.5rem;"><?php echo $data['icon']; ?></span>
                                <h4 class="u-margin-none"
                                    style="font-size: 1.1rem;"><?php echo $data['label']; ?></h4>
                            </div>

                            <div class="c-card__body u-flex u-flex-column u-gap-m"
                                 style="padding: 1.25rem 1.5rem;">
                                <div class="u-flex u-gap-s u-width-100">
                                    <form method="POST"
                                          onsubmit="return confirm('Daten nach MySQL migrieren?')"
                                          class="u-width-100">
                                        <input type="hidden"
                                               name="action"
                                               value="migrate_data">
                                        <input type="hidden"
                                               name="target"
                                               value="<?php echo $key; ?>">
                                        <input type="hidden"
                                               name="direction"
                                               value="json_to_mysql">
                                        <button type="submit"
                                                class="c-button c-button--secondary u-width-100"
                                                style="font-size: 0.85rem;"
                                                <?php echo ! $auth->hasPermission("dashboard.migration.{$key}.json_to_mysql") ? 'disabled title="Keine Berechtigung"' : ''; ?>>
                                            JSON ➔ SQL
                                        </button>
                                    </form>

                                    <form method="POST"
                                          onsubmit="return confirm('Daten nach JSON migrieren?')"
                                          class="u-width-100">
                                        <input type="hidden"
                                               name="action"
                                               value="migrate_data">
                                        <input type="hidden"
                                               name="target"
                                               value="<?php echo $key; ?>">
                                        <input type="hidden"
                                               name="direction"
                                               value="mysql_to_json">
                                        <button type="submit"
                                                class="c-button c-button--secondary u-width-100"
                                                style="font-size: 0.85rem;"
                                                <?php echo ! $auth->hasPermission("dashboard.migration.{$key}.mysql_to_json") ? 'disabled title="Keine Berechtigung"' : ''; ?>>
                                            SQL ➔ JSON
                                        </button>
                                    </form>
                                </div>

                                <form method="POST"
                                      class="u-width-100">
                                    <input type="hidden"
                                           name="action"
                                           value="migrate_data">
                                    <input type="hidden"
                                           name="target"
                                           value="<?php echo $key; ?>">
                                    <input type="hidden"
                                           name="direction"
                                           value="sync">
                                    <button type="submit"
                                            class="c-button c-button--primary u-width-100"
                                            style="font-size: 0.9rem; font-weight: 600;"
                                            <?php echo ! $auth->hasPermission("dashboard.migration.{$key}.sync") ? 'disabled title="Keine Berechtigung"' : ''; ?>>
                                        🔄 Zusammenführen (Sync)
                                    </button>
                                </form>
                            </div>
                        </div>
                    <?php } ?>
                </div>
            </div>
        <?php } ?>

        <?php if ($auth->hasPermission('dashboard.migration.backups.view')) { ?>
            <div class="u-margin-top-xl">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">💾</span>
                    <div>
                        <h3 class="u-margin-none">Verfügbare Backups</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Übersicht aller Systemsicherungen zur
                            Wiederherstellung.</p>
                    </div>
                </header>

                <div class="c-card"
                     style="border-top: 4px solid var(--json-color);">
                    <div class="c-card__body u-padding-none">
                        <?php if (empty($backups)) { ?>
                            <div class="u-padding-around-l u-text-center u-text-muted">
                                <span style="font-size: 2rem; display: block; margin-bottom: 10px;">📦</span>
                                Es sind noch keine Backups im System vorhanden.
                            </div>
                        <?php } else { ?>
                            <div class="u-flex u-flex-column">
                                <?php
                                    $isFirst = true;
                            foreach ($backups as $ts => $files) {
                                $dateObj       = \DateTimeImmutable::createFromFormat('Ymd-His', $ts);
                                $formattedDate = $dateObj ? $dateObj->format('d.m.Y — H:i:s') : $ts;
                                ?>
                                    <div class="u-flex u-flex-wrap u-align-center u-justify-between u-gap-m"
                                         style="padding: 1.25rem 1.5rem; <?php echo ! $isFirst ? 'border-top: 1px solid var(--border-color);' : ''; ?>">

                                        <div class="u-flex u-flex-column u-gap-xs"
                                             style="flex: 1; min-width: 250px;">
                                            <strong class="u-font-l"><?php echo $formattedDate; ?></strong>
                                            <div class="u-flex u-flex-wrap u-gap-xs u-margin-top-xs">
                                                <?php foreach ($files as $file) {
                                                    // Farbe basierend auf Dateityp (SQL oder JSON)
                                                    $badgeColor = \str_contains($file, 'sql.json') ? 'var(--primary-color)' : 'var(--json-color)';
                                                    $cleanName  = \str_replace(['_file.json', '_sql.json'], '', $file);
                                                    ?>
                                                    <span class="c-badge" style="background: <?php echo $badgeColor; ?>; font-size: 0.7rem;">
                                                <?php echo \htmlspecialchars($cleanName); ?>
                                            </span>
                                                <?php } ?>
                                            </div>
                                        </div>

                                        <div style="min-width: 320px;">
                                            <?php if ($auth->hasPermission('dashboard.migration.restore.execute')) { ?>
                                                <form method="POST"
                                                      onsubmit="return confirm('ACHTUNG: Dies überschreibt Ihre aktuellen Daten am Zielort! Fortfahren?')"
                                                      class="u-flex u-gap-s u-align-center u-justify-end">
                                                    <input type="hidden"
                                                           name="action"
                                                           value="restore_data">
                                                    <input type="hidden"
                                                           name="timestamp"
                                                           value="<?php echo $ts; ?>">

                                                    <select name="target"
                                                            class="c-form-select"
                                                            style="max-width: 200px;"
                                                            required>
                                                        <option value="">-- Zielbereich wählen --</option>
                                                        <?php foreach ($targets as $key => $data) { ?>
                                                            <option value="<?php echo $key; ?>"><?php echo $data['label']; ?></option>
                                                        <?php } ?>
                                                    </select>

                                                    <button type="submit"
                                                            class="c-button c-button--danger"
                                                            style="width: auto; padding: 0 16px; font-weight: 600;">
                                                        Wiederherstellen
                                                    </button>
                                                </form>
                                            <?php } else { ?>
                                                <span class="u-text-muted u-float-right u-text-small">🚫 Keine Restore-Rechte</span>
                                            <?php } ?>
                                        </div>
                                    </div>
                                <?php
                                    $isFirst = false;
                            } ?>
                            </div>
                        <?php } ?>
                    </div>
                </div>
            </div>
        <?php } ?>

        <?php if ($auth->hasPermission('dashboard.admin-tools.view')) { ?>
            <div class="u-margin-top-xl">
                <header class="u-margin-bottom-m u-flex u-align-center u-gap-s">
                    <span class="u-font-xl">🛠️</span>
                    <div>
                        <h3 class="u-margin-none">Gefahrenzone: System-Werkzeuge</h3>
                        <p class="u-text-muted u-text-small u-margin-none">Cache leeren und Datenbestände zurücksetzen.
                        </p>
                    </div>
                </header>

                <div class="u-grid"
                     style="grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem;">

                    <?php if ($auth->hasPermission('dashboard.migration.delete-cache.execute')) { ?>
                    <div class="c-card"
                         style="margin-bottom: 0; border-top: 4px solid var(--danger-color);">
                        <div class="c-card__header u-flex u-align-center u-gap-s"
                             style="padding: 1rem 1.5rem;">
                            <h4 class="u-margin-none"
                                style="font-size: 1.1rem;">🧹 System-Cache</h4>
                        </div>
                        <div class="c-card__body u-flex u-flex-column u-gap-m"
                             style="padding: 1.25rem 1.5rem;">
                            <p class="u-text-small u-margin-none">Löscht temporäre Dateien (wie z.B. Deptrac Caches oder
                                kompilierte Rechte). Behebt oft Anzeige-Bugs.</p>
                            <form method="POST"
                                  onsubmit="return confirm('Cache jetzt leeren?')"
                                  class="u-margin-top-auto">
                                <input type="hidden"
                                       name="action"
                                       value="clear_cache">
                                <input type="hidden"
                                       name="csrf_token"
                                       value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">
                                <button type="submit"
                                        class="c-button c-button--secondary u-width-100"
                                        style="font-size: 0.9rem;">
                                    Cache leeren
                                </button>
                            </form>
                        </div>
                    </div>
                    <?php } ?>

                    <?php if ($auth->hasPermission('dashboard.migration.delete-data.execute')) { ?>
                        <div class="c-card"
                             style="margin-bottom: 0; border-top: 4px solid var(--danger-color);">
                            <div class="c-card__header u-flex u-align-center u-gap-s"
                                 style="padding: 1rem 1.5rem;">
                                <h4 class="u-margin-none"
                                    style="font-size: 1.1rem;">🗑️ Datenbestand löschen</h4>
                            </div>
                            <div class="c-card__body u-flex u-flex-column u-gap-m"
                                 style="padding: 1.25rem 1.5rem;">
                                <p class="u-text-small u-margin-none">Leert eine komplette Tabelle oder JSON-Datei. Es
                                    wird automatisch ein Sicherungs-Backup erstellt.</p>
                                <form method="POST"
                                      onsubmit="return confirm('WARNUNG: Alle Daten dieses Bereichs werden GELÖSCHT! Fortfahren?')"
                                      class="u-margin-top-auto">
                                    <input type="hidden"
                                           name="action"
                                           value="truncate_target">
                                    <input type="hidden"
                                           name="csrf_token"
                                           value="<?php echo $_SESSION['csrf_token'] ?? ''; ?>">

                                    <div class="u-flex u-gap-s">
                                        <select name="target"
                                                class="c-form-select"
                                                required>
                                            <option value="">-- Bereich wählen --</option>
                                            <?php foreach ($targets as $key => $data) { ?>
                                                <option value="<?php echo $key; ?>"><?php echo $data['label']; ?></option>
                                            <?php } ?>
                                        </select>
                                        <button type="submit"
                                                class="c-button c-button--danger"
                                                style="width: auto; padding: 0 16px;">
                                            Löschen
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    <?php } ?>

                </div>
            </div>
        <?php } ?>

    </div>
<?php } ?>
