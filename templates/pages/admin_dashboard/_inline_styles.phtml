<?php
/**
 * Partial: Inline Styles
 * Path: templates/pages/admin_dashboard/_inline_styles.phtml
 */
?>
<style>
        /* TABS LOGIK */
        .c-tabs__nav {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
            overflow-x: auto;
        }

        .c-tabs__btn {
            padding: 10px 20px;
            border: none;
            background: #f0f0f0;
            cursor: pointer;
            border-radius: 5px;
            font-weight: bold;
            white-space: nowrap;
            transition: all 0.2s;
        }

        .c-tabs__btn:hover {
            background: #e0e0e0;
        }

        .c-tabs__btn--active {
            background: #3498db;
            color: white;
        }

        .c-tabs__content {
            display: none;
        }

        .c-tabs__content--active {
            display: block;
            animation: fadeIn 0.3s;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        /* ZUSTÄNDE & BADGES */
        .u-bg-suspended {
            background-color: #f8d7da !important;
            border-left: 5px solid #c0392b !important;
        }

        .u-opacity-50 {
            opacity: 0.5;
        }

        .c-table td {
            vertical-align: middle;
        }

        /* DASHBOARD SPEZIFISCH */
        .qr-mini {
            width: 70px;
            height: 70px;
            border: 1px solid #ccc;
            background: #fff;
        }

        .stats-box {
            padding: 15px;
            border-radius: 8px;
        }
    </style>
