<?php

/**
 * Partial: Dashboard Tab - Data Export & Filters
 * @var string $filterStart
 * @var string $filterEnd
 */
?>
<div class="u-grid u-gap-l" style="grid-template-columns: 1fr 1fr;">
    <!-- Linke Seite: Filter & Suche -->
    <section class="c-card">
        <header class="c-card__header">
            <h3 class="u-margin-none">🔍 Ansicht filtern</h3>
            <p class="u-text-muted">Zeitraum und Suche für alle Listen anpassen.</p>
        </header>
        <div class="c-card__body">
            <form method="GET" class="c-form">
                <div class="u-flex u-gap-s">
                    <div class="c-form-group u-width-100">
                        <label class="c-form-label">Von:</label>
                        <input type="date" name="start" value="<?php echo $filterStart; ?>" class="c-form-input">
                    </div>
                    <div class="c-form-group u-width-100">
                        <label class="c-form-label">Bis:</label>
                        <input type="date" name="end" value="<?php echo $filterEnd; ?>" class="c-form-input">
                    </div>
                </div>
                <button type="submit" class="c-button c-button--primary u-width-100 u-margin-top-m">
                    Filter-Zeitraum anwenden
                </button>
            </form>

            <hr class="u-margin-y-l" style="opacity: 0.1;">

            <div class="c-form-group">
                <label class="c-form-label">Schnellsuche (Live-Filter)</label>
                <div class="u-position-relative">
                    <input type="text" id="adminSearch"
                        placeholder="Name, Kennzeichen oder Code..."
                        title="Suchen Sie nach Namen, KFZ-Kennzeichen oder dem SmartCode"
                        class="c-form-input c-form-input--search">
                </div>
            </div>
        </div>
    </section>

    <!-- Rechte Seite: Export Sektion -->
    <section class="c-card">
        <header class="c-card__header">
            <h3 class="u-margin-none">📤 Daten-Export</h3>
            <p class="u-text-muted">Wählen Sie das gewünschte Format für den Download.</p>
        </header>
        <div class="c-card__body">
            <!-- Das neue flexible Grid -->
            <div class="c-export-grid">
                <!-- CSV Export Button -->
                <a href="?export=csv&start=<?php echo $filterStart; ?>&end=<?php echo $filterEnd; ?>" class="c-export-card c-export-card--csv">
                    <div class="c-export-card__icon">📊</div>
                    <div class="c-export-card__content">
                        <span class="c-export-card__title">Excel / CSV</span>
                        <span class="c-export-card__desc">Tabellenkalkulation</span>
                    </div>
                </a>

                <!-- JSON Export Button -->
                <a href="?export=json&start=<?php echo $filterStart; ?>&end=<?php echo $filterEnd; ?>" class="c-export-card c-export-card--json">
                    <div class="c-export-card__icon">⚙️</div>
                    <div class="c-export-card__content">
                        <span class="c-export-card__title">JSON Daten</span>
                        <span class="c-export-card__desc">Technischer Import</span>
                    </div>
                </a>

                <!-- Hier könnten zukünftig weitere Buttons stehen (z.B. PDF Export) -->
            </div>

            <div class="u-margin-top-l u-padding-s c-export-notice">
                <strong>Gefilterter Zeitraum:</strong><br>
                <?php echo \date('d.m.Y', \strtotime($filterStart)); ?> bis <?php echo \date('d.m.Y', \strtotime($filterEnd)); ?>
            </div>
        </div>
    </section>
</div>
