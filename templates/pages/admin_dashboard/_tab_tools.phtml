<?php
/**
 * Partial: Dashboard Tab - Admin Tools
 * Path: templates/pages/admin_dashboard/_tab_tools.phtml
 *
 * @var \App\Infrastructure\Config\Config $config
 * @var array                             $reasons
 */
?>
<div class="c-tab-content-container">
    <div class="u-grid u-gap-l" style="grid-template-columns: 1fr 1fr;">

        <!-- Gutschein Erstellung -->
        <section class="c-card">
            <header class="c-card__header">
                <h3 class="u-margin-none">🎟️ Gutschein erzeugen</h3>
                <p class="u-text-muted">Einen QR-Code/Link für eine 100% rabattierte Buchung erstellen.</p>
            </header>
            <div class="c-card__body">
                <form method="POST" class="c-form">
                    <input type="hidden" name="action" value="create_voucher">
                    <div class="c-form-group">
                        <label class="c-form-label">Interne Bezeichnung</label>
                        <input type="text" name="reason_internal" class="c-form-input" required placeholder="z.B. Vorab Bar bezahlt">
                    </div>
                    <div class="c-form-group u-margin-top-s">
                        <label class="c-form-label">Gültig für Vorlage</label>
                        <select name="template_key" class="c-form-select">
                            <?php foreach ($config->get('permit_templates', []) as $key => $tpl) { ?>
                                <option value="<?php echo $key; ?>"><?php echo $tpl['label']; ?></option>
                            <?php } ?>
                        </select>
                    </div>

                    <div class="u-margin-top-m u-padding-around-m" style="background: #f8fafc; border: 1px solid var(--border-color); border-radius: 8px;">
                        <p class="u-font-bold u-margin-bottom-s u-margin-top-none" style="font-size: 0.9rem;">Formular-Vorbefüllung (Optional)</p>
                        <div class="u-flex u-gap-s">
                            <input type="text" name="pre_name" class="c-form-input" placeholder="Pächter Name">
                            <input type="text" name="pre_parzelle" class="c-form-input" style="width:100px" placeholder="Parz.">
                        </div>
                        <input type="text" name="pre_kennzeichen" class="c-form-input u-margin-top-s" placeholder="Kennzeichen (Optional)">
                    </div>
                    <button type="submit" class="c-button c-button--primary u-width-100 u-margin-top-m">Gutschein & QR-Code generieren</button>
                </form>
            </div>
        </section>

        <!-- Manuelle Erstellung -->
        <section class="c-card">
            <header class="c-card__header">
                <h3 class="u-margin-none">⚡ Direkte Genehmigung</h3>
                <p class="u-text-muted">Sofortige Ausstellung (ohne Nutzer-Verifizierung).</p>
            </header>
            <div class="c-card__body">
                <form method="POST" class="c-form">
                    <input type="hidden" name="action" value="create_manual">
                    <div class="c-form-group">
                        <label class="c-form-label">Vorlage</label>
                        <select name="template_key" id="manual_template_key" class="c-form-select">
                            <?php foreach ($config->get('permit_templates', []) as $key => $tpl) { ?>
                                <option value="<?php echo $key; ?>"><?php echo $tpl['label']; ?></option>
                            <?php } ?>
                        </select>
                    </div>
                    <div class="u-flex u-gap-s u-margin-top-s">
                        <div style="flex:1">
                            <label class="c-form-label">Startdatum</label>
                            <input type="date" name="datum_von" class="c-form-input" value="<?php echo \date('Y-m-d'); ?>">
                        </div>
                        <div style="flex:1; display:none;" id="custom_end_wrapper">
                            <label class="c-form-label">Enddatum</label>
                            <input type="date" name="datum_bis" class="c-form-input" value="<?php echo \date('Y-m-d', \strtotime('+1 year')); ?>">
                        </div>
                    </div>

                    <div class="u-margin-top-m u-padding-around-m" style="background: #f8fafc; border: 1px solid var(--border-color); border-radius: 8px;">
                        <input type="text" name="name" class="c-form-input u-margin-bottom-s" placeholder="Pächter Name" required>
                        <div class="u-flex u-gap-s u-margin-bottom-s">
                            <input type="text" name="parzelle" class="c-form-input" placeholder="Parzelle" required>
                            <input type="text" name="kennzeichen" class="c-form-input" placeholder="Kennzeichen">
                        </div>
                        <input type="email" name="email" class="c-form-input" placeholder="E-Mail Adresse (Optional)">
                    </div>

                    <div class="c-form-group u-margin-top-m">
                        <label class="c-form-label">Interner Vermerk / Grund</label>
                        <select name="reason" class="c-form-select">
                            <?php foreach ($reasons as $r) { ?>
                                <option value="<?php echo \htmlspecialchars($r); ?>"><?php echo \htmlspecialchars($r); ?></option>
                            <?php } ?>
                        </select>
                    </div>
                    <button type="submit" class="c-button c-button--primary u-width-100 u-margin-top-m">Genehmigung sofort ausstellen</button>
                </form>
            </div>
        </section>

    </div>
</div>
