<?php

/**
 * Partial: Dashboard Tab - Universal Admin Tools (Complete & Optimized)
 * 
 * Path: templates/pages/admin_dashboard/_tab_tools.phtml
 *
 * @var \App\Infrastructure\Config\Config $config
 * @var array                             $reasons
 */

// Vorbereitung der Template-Daten für JavaScript
$tplMetadata = [];
foreach ($config->get('permit_templates', []) as $key => $tpl) {
    $tplMetadata[$key] = [
        'days' => $tpl['days'],
        'type' => $tpl['type'],
    ];
}
?>
<div class="c-tab-content-container">
    <section class="c-card" style="max-width: 800px; margin: 0 auto;">
        <header class="c-card__header u-text-center">
            <div style="font-size: 2.5rem; margin-bottom: 10px;">🛠️</div>
            <h3 class="u-margin-none">Universal-Generator</h3>
            <p class="u-text-muted">Zeiträume planen, Gutscheine erzeugen oder Genehmigungen sofort ausstellen.</p>
        </header>

        <div class="c-card__body">
            <!-- Warnung-Box für Datumskorrekturen -->
            <div id="date-warning" class="u-margin-bottom-m" style="display:none; background: #fffbeb; border: 1px solid #fcd34d; color: #92400e; padding: 12px; border-radius: 8px; font-size: 0.9rem;">
                <div style="display:flex; gap: 10px; align-items: center;">
                    <span>💡</span>
                    <span id="date-warning-text"></span>
                </div>
            </div>

            <form id="unifiedToolsForm" method="POST" class="c-form">

                <!-- ABSCHNITT 1: RAHMENBEDINGUNGEN -->
                <div style="background: var(--bg-light); padding: 1.5rem; border-radius: 12px; border: 1px solid var(--border-color); margin-bottom: 1.5rem;">
                    <h4 class="u-margin-top-none u-margin-bottom-m" style="color: var(--primary-color); display: flex; align-items: center; gap: 8px;">
                        <span style="background: var(--primary-color); color: white; width: 24px; height: 24px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; font-size: 0.8rem;">1</span>
                        Zeitraum & Vorlage
                    </h4>

                    <div class="c-form-group u-margin-bottom-m">
                        <label class="c-form-label">Wählen Sie die Vorlage</label>
                        <select name="template_key" id="u_template_key" class="c-form-select">
                            <?php foreach ($config->get('permit_templates', []) as $key => $tpl) { ?>
                                <option value="<?php echo $key; ?>"><?php echo $tpl['label']; ?></option>
                            <?php } ?>
                        </select>
                    </div>

                    <div class="u-grid u-gap-m" style="grid-template-columns: 1fr 1fr;">
                        <div class="c-form-group">
                            <label class="c-form-label">Startdatum (Von)</label>
                            <input type="date" name="datum_von" id="u_datum_von" class="c-form-input" value="<?php echo \date('Y-m-d'); ?>">
                        </div>
                        <div class="c-form-group">
                            <label class="c-form-label">Enddatum (Bis)</label>
                            <input type="date" name="datum_bis" id="u_datum_bis" class="c-form-input">
                        </div>
                    </div>

                    <div class="c-form-group u-margin-top-m">
                        <label class="c-form-label">Interne Bezeichnung / Grund <small class="u-text-muted">(Pflicht f. Gutschein)</small></label>
                        <input type="text" name="reason" id="u_reason" class="c-form-input" list="reason_presets" placeholder="z.B. Barzahlung beim Vorstand">
                        <datalist id="reason_presets">
                            <?php foreach ($reasons as $r) { ?>
                                <option value="<?php echo \htmlspecialchars($r); ?>">
                                <?php } ?>
                        </datalist>
                    </div>
                </div>

                <!-- ABSCHNITT 2: PÄCHTER-DATEN -->
                <div style="background: var(--bg-light); padding: 1.5rem; border-radius: 12px; border: 1px solid var(--border-color); margin-bottom: 1.5rem;">
                    <h4 class="u-margin-top-none u-margin-bottom-m" style="color: var(--primary-color); display: flex; align-items: center; gap: 8px;">
                        <span style="background: var(--primary-color); color: white; width: 24px; height: 24px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; font-size: 0.8rem;">2</span>
                        Pächter-Informationen
                    </h4>

                    <div class="u-grid u-gap-m" style="grid-template-columns: 2fr 1fr;">
                        <div class="c-form-group">
                            <label class="c-form-label">Name <small class="u-text-muted">(Pflicht f. Sofort-Gen.)</small></label>
                            <input type="text" name="name" id="u_name" class="c-form-input" placeholder="Max Mustermann">
                        </div>
                        <div class="c-form-group">
                            <label class="c-form-label">Parzelle <small class="u-text-muted">(Pflicht f. Sofort-Gen.)</small></label>
                            <input type="text" name="parzelle" id="u_parzelle" class="c-form-input" placeholder="0000">
                        </div>
                    </div>
                    <div class="c-form-group u-margin-top-m">
                        <label class="c-form-label">E-Mail-Adresse <small class="u-text-muted">(Optional)</small></label>
                        <input type="email" name="email" id="u_email" class="c-form-input" placeholder="mail@beispiel.de">
                    </div>
                </div>

                <!-- ABSCHNITT 3: FAHRZEUG-DETAILS -->
                <div style="background: var(--bg-light); padding: 1.5rem; border-radius: 12px; border: 1px solid var(--border-color); margin-bottom: 2rem;">
                    <h4 class="u-margin-top-none u-margin-bottom-m" style="color: var(--primary-color); display: flex; align-items: center; gap: 8px;">
                        <span style="background: var(--primary-color); color: white; width: 24px; height: 24px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; font-size: 0.8rem;">3</span>
                        Fahrzeug-Details
                    </h4>

                    <div class="u-grid u-gap-m" style="grid-template-columns: 1fr 1fr;">
                        <div class="c-form-group">
                            <label class="c-form-label">Typ</label>
                            <select name="typ" id="u_typ" class="c-form-select">
                                <?php foreach ($config->get('vehicle_types', []) as $val => $label) { ?>
                                    <option value="<?php echo $val; ?>"><?php echo $label; ?></option>
                                <?php } ?>
                            </select>
                        </div>
                        <div class="c-form-group">
                            <label class="c-form-label">Amtl. Kennzeichen</label>
                            <input type="text" name="kennzeichen" id="u_kennzeichen" class="c-form-input" placeholder="B AB 1234">
                        </div>
                    </div>

                    <div class="u-grid u-gap-m u-margin-top-m" style="grid-template-columns: 1fr 1fr;">
                        <div class="c-form-group" id="u_firma_wrapper">
                            <label class="c-form-label">Firma / Lieferant</label>
                            <input type="text" name="firma" id="u_firma" class="c-form-input" placeholder="Müller Bau GmbH">
                        </div>
                        <div class="c-form-group">
                            <label class="c-form-label">Zweck</label>
                            <select name="zweck" id="u_zweck" class="c-form-select">
                                <option value="">-- Keine Angabe --</option>
                                <?php foreach ($config->get('purposes', []) as $val => $label) { ?>
                                    <option value="<?php echo $val; ?>"><?php echo $label; ?></option>
                                <?php } ?>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- AKTIONEN -->
                <div class="u-flex u-gap-m u-justify-between" style="flex-wrap: wrap;">
                    <button type="button" class="c-button" style="flex: 1; min-width: 250px; background: #6366f1; color: white; padding: 1.25rem; border-radius: 12px; border: none; display: flex; align-items: center; justify-content: center; gap: 15px; transition: all 0.2s; cursor: pointer;" onclick="submitUnifiedForm('create_voucher')" onmouseover="this.style.transform='translateY(-2px)'" onmouseout="this.style.transform='translateY(0)'">
                        <span style="font-size: 2rem;">🎟️</span>
                        <div style="text-align: left;">
                            <span style="display:block; font-weight: 800; font-size: 1.1rem;">Gutschein erzeugen</span>
                            <span style="font-size: 0.8rem; opacity: 0.9;">QR-Code erstellen & Zeitraum vorwählen</span>
                        </div>
                    </button>

                    <button type="button" class="c-button" style="flex: 1; min-width: 250px; background: #10b981; color: white; padding: 1.25rem; border-radius: 12px; border: none; display: flex; align-items: center; justify-content: center; gap: 15px; transition: all 0.2s; cursor: pointer;" onclick="submitUnifiedForm('create_manual')" onmouseover="this.style.transform='translateY(-2px)'" onmouseout="this.style.transform='translateY(0)'">
                        <span style="font-size: 2rem;">⚡</span>
                        <div style="text-align: left;">
                            <span style="display:block; font-weight: 800; font-size: 1.1rem;">Sofort ausstellen</span>
                            <span style="font-size: 0.8rem; opacity: 0.9;">Genehmigung sofort aktiv schalten</span>
                        </div>
                    </button>
                </div>
            </form>
        </div>
    </section>
</div>

<script>
    const templates = <?php echo \json_encode($tplMetadata); ?>;
    const inputVon = document.getElementById('u_datum_von');
    const inputBis = document.getElementById('u_datum_bis');
    const selectTpl = document.getElementById('u_template_key');
    const selectTyp = document.getElementById('u_typ');
    const firmaWrapper = document.getElementById('u_firma_wrapper');
    const warningBox = document.getElementById('date-warning');
    const warningText = document.getElementById('date-warning-text');

    /**
     * Fahrzeug-Typ Logik (Firma ein/ausblenden)
     */
    function toggleFirmaField() {
        if (selectTyp.value === 'lkw') {
            firmaWrapper.style.display = 'block';
        } else {
            firmaWrapper.style.display = 'none';
            document.getElementById('u_firma').value = ''; // Feld leeren wenn versteckt
        }
    }

    /**
     * Datums-Warnung
     */
    function showDateWarning(msg) {
        if (!msg) {
            warningBox.style.display = 'none';
            return;
        }
        warningText.innerText = msg;
        warningBox.style.display = 'block';
    }

    function getTodayStr() {
        const d = new Date();
        d.setHours(0, 0, 0, 0);
        return d.toISOString().split('T')[0];
    }

    /**
     * Bi-Direktionale Berechnung
     */
    function handleDateChange(source) {
        const config = templates[selectTpl.value];
        const today = getTodayStr();

        showDateWarning(null);

        // Spezialzeitraum: Keine Automatik, alles frei
        if (config.days === 'custom') {
            return;
        }

        const duration = parseInt(config.days);

        if (source === 'von') {
            if (inputVon.value) {
                let vonDate = new Date(inputVon.value);
                let bisDate = new Date(vonDate);
                bisDate.setDate(vonDate.getDate() + duration);
                inputBis.value = bisDate.toISOString().split('T')[0];
            }
        } else if (source === 'bis') {
            if (inputBis.value) {
                let bisDate = new Date(inputBis.value);
                let vonDate = new Date(bisDate);
                vonDate.setDate(bisDate.getDate() - duration);

                let vonStr = vonDate.toISOString().split('T')[0];

                if (vonStr < today) {
                    inputVon.value = today;
                    showDateWarning("Hinweis: Das Startdatum wurde auf heute korrigiert, da Genehmigungen nicht in der Vergangenheit beginnen können. Der Zeitraum wurde entsprechend verkürzt.");
                } else {
                    inputVon.value = vonStr;
                }
            }
        }
    }

    // Event-Listener
    selectTpl.addEventListener('change', () => handleDateChange('von'));
    selectTyp.addEventListener('change', toggleFirmaField);
    inputVon.addEventListener('change', () => handleDateChange('von'));
    inputBis.addEventListener('change', () => handleDateChange('bis'));

    // Initialisierung
    handleDateChange('von');
    toggleFirmaField();

    /**
     * Formular-Absendung
     */
    function submitUnifiedForm(actionType) {
        const form = document.getElementById('unifiedToolsForm');
        const reasonField = document.getElementById('u_reason');
        const nameField = document.getElementById('u_name');
        const parzelleField = document.getElementById('u_parzelle');

        [reasonField, nameField, parzelleField].forEach(f => f.setCustomValidity(''));

        if (actionType === 'create_voucher' && !reasonField.value.trim()) {
            reasonField.setCustomValidity('Eine interne Bezeichnung ist für Gutscheine zwingend erforderlich.');
            reasonField.reportValidity();
            return;
        }

        if (actionType === 'create_manual' && (!nameField.value.trim() || !parzelleField.value.trim())) {
            alert('Name und Parzelle müssen für eine Sofort-Genehmigung ausgefüllt sein.');
            return;
        }

        const hiddenAction = document.createElement('input');
        hiddenAction.type = 'hidden';
        hiddenAction.name = 'action';
        hiddenAction.value = actionType;
        form.appendChild(hiddenAction);
        form.submit();
    }
</script>
