<?php

/**
 * Snippet: Verification Permit Details
 *
 * Universelle Status-Box für Fahrzeuge und Sperrgründe.
 *
 * Path: templates/pages/check/_details.phtml
 *
 * SPDX-License-Identifier: LicenseRef-Proprietary
 *
 * @var \App\Core\Entity\Permit $permit
 */
?>
<div
     style="background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 12px; padding: 20px; text-align: left; margin: 25px 0;">
    <div class="u-margin-bottom-m">
        <span class="u-text-muted u-text-small u-font-bold" style="text-transform: uppercase; letter-spacing: 1px;">Fahrzeug</span><br>
        <strong style="font-size: 1.15rem; color: #1e293b;">
            <?php
            $info = [];
if (! empty($permit->getLicensePlate())) {
    $info[] = \htmlspecialchars($permit->getLicensePlate());
}
if (! empty($permit->getCompany())) {
    $info[] = 'Firma: ' . \htmlspecialchars($permit->getCompany());
}
echo \implode(' / ', $info);
?>
        </strong>
    </div>

    <div class="u-border-top u-padding-top-m <?php echo $permit->isSuspended() ? 'u-margin-bottom-m' : ''; ?>">
        <span class="u-text-muted u-text-small u-font-bold" style="text-transform: uppercase; letter-spacing: 1px;">Gültigkeit</span><br>
        <span style="font-size: 1.05rem; color: #334155;">
            Von <strong><?php echo $permit->getValidFrom()->format('d.m.Y'); ?></strong> bis <strong><?php echo $permit->getValidUntil()->format('d.m.Y'); ?></strong>
        </span>
    </div>

    <?php if ($permit->isSuspended() && ! empty($permit->getSuspensionReason())) { ?>
        <div class="u-border-top u-padding-top-m"
             style="border-top-color: #fee2e2;">
            <span class="u-text-small u-font-bold" style="text-transform: uppercase; letter-spacing: 1px; color: var(--danger-color);">Sperrgrund</span><br>
            <span style="font-size: 1rem; color: #991b1b; font-weight: 600;">
                <?php echo \htmlspecialchars($permit->getSuspensionReason()); ?>
            </span>
        </div>
    <?php } ?>
</div>
