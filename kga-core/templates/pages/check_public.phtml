<div class="c-card c-card--check">
    <div class="c-card__header">
        <h2>Status-Prüfung</h2>
    </div>
    <div class="c-card__body u-text-center">
        <?php if ($permit->status === 'bezahlt' && $permit->isValid()): ?>
            <div class="u-text-success">✅ DIESE GENEHMIGUNG IST GÜLTIG</div>
        <?php else: ?>
            <div class="u-text-danger">❌ UNGÜLTIG ODER NOCH NICHT BEZAHLT</div>
        <?php endif; ?>

        <hr>
        <p><strong>Fahrzeug:</strong> <?= htmlspecialchars($permit->kennzeichen) ?></p>
        <p><strong>Zeitraum:</strong> <?= $permit->von->format('d.m.Y') ?> - <?= $permit->bis->format('d.m.Y') ?></p>
    </div>
</div>
