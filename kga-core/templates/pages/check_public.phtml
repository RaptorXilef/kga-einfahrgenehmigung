<!DOCTYPE html>
<html lang="de" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Status-Prüfung - <?= htmlspecialchars($settings['vereins_name']) ?></title>
    <link rel="stylesheet" href="assets/css/main.min.css">
</head>
<body class="l-check">
    <main class="c-container u-text-center">
        <header class="c-page-header">
            <h1 class="c-page-header__title">Einfahrgenehmigung</h1>
        </header>

        <section class="c-check-card <?= ($permit->status === 'bezahlt' && $permit->isValid()) ? 'c-check-card--valid' : 'c-check-card--invalid' ?>">
            <div class="c-check-card__status">
                <?php if ($permit->status === 'bezahlt' && $permit->isValid()): ?>
                    <span class="c-icon c-icon--success">✅</span>
                    <h2 class="c-check-card__title">GÜLTIG</h2>
                <?php else: ?>
                    <span class="c-icon c-icon--error">❌</span>
                    <h2 class="c-check-card__title">UNGÜLTIG / WARTEND</h2>
                <?php endif; ?>
            </div>

            <div class="c-check-card__details">
                <div class="c-detail-row">
                    <span class="c-detail-row__label">Kennzeichen:</span>
                    <span class="c-detail-row__value"><?= htmlspecialchars($permit->kennzeichen) ?></span>
                </div>
                <div class="c-detail-row">
                    <span class="c-detail-row__label">Gültig bis:</span>
                    <span class="c-detail-row__value"><?= $permit->bis->format('d.m.Y') ?></span>
                </div>
            </div>

            <footer class="c-check-card__footer">
                <p class="u-text-muted">ID: <?= htmlspecialchars($permit->code) ?></p>
            </footer>
        </section>
    </main>
</body>
</html>
